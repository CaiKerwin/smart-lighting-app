# 智能控制开发指导文档
---

## 1. 模块概述与页面组成

光控模块是一个列表页（`LuxListFragment`），由两部分组成：

| 区域 | 内容 | 说明 |
|------|------|------|
| 光控数据面板 | `recyclerLux`（2 列网格） | 展示所有光控设备（照度/温度/湿度/内外置） |
| 一键开关灯分组 | `recyclerFastControl`（3 列网格）+「配置分组」按钮 | 展示开关灯分组，点击进入操作，长按删除 |

其余页面：

| 页面 | 说明 |
|------|------|
| `LuxHistoryActivity` | 光控历史数据（按时间范围查询） |
| `FastGroupManageActivity` | 一键开关灯分组管理（新增/删除/添加通道/添加单灯） |
| `FastControlDetailActivity` | 一键开关灯详情（通道/单灯 Tab + 开关灯操作） |
| `FastAddChannelActivity` | 向分组添加通道 |
| `FastAddLightActivity` | 向分组添加单灯 |

---

## 2. 术语与常量对照

### 2.1 权限码（`OperationCode`，用于按钮级权限校验）

| 常量 | 值 | 用途 |
|------|----|------|
| `FAST_CONTROL` | `"occ"` | 一键开关灯操作权限（开/关/调光、添加分组） |
| `FAST_CHECK` | `"ocv"` | 查看分组详情权限 |
| `FAST_DELETE` | `"lgd1"` | 删除分组权限 |

> `checkPermission(code)` 校验当前用户是否拥有对应操作权限，无权限则不执行/不显示相应操作。

### 2.2 页面类型 `PAGE_TYPE`（进入 FastControlDetailActivity 时）

| 值 | 含义 | 入口 |
|----|------|------|
| 1 | 通道列表（带一键开/关灯按钮） | LuxListFragment 点击分组 |
| 2 | 单灯列表（隐藏操作按钮） | FastGroupManageActivity 点击分组 |

### 2.3 命令状态码（`OrderUtil.getResult`）

| status | 文案 | 颜色（`getResultColor`） |
|--------|------|------------------------|
| 2 | 指令已发送 | 正常色 |
| 4 | 执行中 | 正常色 |
| 5 | 已重发 | 橙色 |
| 7 | 指令超时 | 红色 |
| 8 | 执行失败 | 红色 |
| 9 | 执行成功 | 强调色 |

---

## 3. 通用请求 / 响应约定

与其它模块一致（必须复刻）：

- **请求头**：`auth`、`Custid`、`Apptype`、`Lang`。
- **请求体封装**：POST/PUT 统一 `{"content": Base64(业务JSON)}`。
- **响应封装**：`{code, data, abab}`，`code==0` 成功；`abab!="false"` 时 `data` 为 `Base64(AES)` 密文需解密；`data` 为 null/`[]`/`{}` 按「暂无数据」处理。
- 业务失败走 `errorLiveData`，按 `errorUrl` 区分接口。

---

## 4. 数据接口清单

| # | 路径 | 方法 | 用途 | 请求体（业务 JSON） |
|---|------|------|------|---------------------|
| 1 | `/device/lux/QueryState` | POST | 光控设备列表 | `{}`（无 body） |
| 2 | `/device/lux/QueryLogs` | POST | 光控历史数据 | `{"stationId":"<id>","start":"...","end":"...","index":1,"size":20000}` |
| 3 | `/station/lux/QueryOutputGroup` | POST | 一键开关灯分组列表 | `{}` |
| 4 | `/station/lux/SaveOutputGroup` | POST | 新增分组 | `{"id":"","name":"<分组名>"}` |
| 5 | `/station/lux/DeleteOutputGroup/` | POST | 删除分组 | `{"id":"<groupId>"}` |
| 6 | `/station/lux/QueryChannels` | POST | 分组内通道列表 | `{"id":"<groupId>"}` |
| 7 | `/station/lux/QueryLights` | POST | 分组内单灯列表 | `{"id":"<groupId>"}` |
| 8 | `/station/lux/SetChannels` | POST | 向分组添加通道 | `{"id":"<groupId>","channels":[ids]}` |
| 9 | `/station/lux/SetLights` | POST | 向分组添加单灯 | `{"id":"<groupId>","lights":[ids]}` |
| 10 | `/station/config/QueryOutput/` | POST | 分组下的控制通道列表（添加通道页数据源） | `{"groupId":"<groupId>","stationId":0}` |
| 11 | `/station/light/Simple` | POST | 分组下的所有单灯（添加单灯页数据源） | `{"stationId":0,"groupId":"<groupId>"}` |
| 12 | `/station/command/SendOutputOld` | POST | 一键开关灯（通道）命令 | 见 §13 |
| 13 | `/station/command/SendLampOld` | POST | 单灯命令 | 见 §13 |

---

## 5. 光控数据面板（LuxListFragment 主页面）

### 5.1 初始化

```kotlin
initData() {
    mViewModel.getFastGroupList()   // 一键开关灯分组
    mViewModel.getLuxList()         // 光控列表
}
```

### 5.2 光控列表显示（`LuxAdapter`）

`getLuxList()` → `POST /device/lux/QueryState` → `LuxListBean[]`。

每个 Item 显示：

| 字段 | 显示 | 来源字段 |
|------|------|---------|
| 名称 | `name` | `LuxListBean.name` |
| 照度 | `"${luxValue} lux"` | `luxValue` |
| 采集时间 | `fireTime` | `fireTime` |
| 温度 | `"${temperature} ℃"` | `temperature` |
| 湿度 | `"${humidity} %"` | `humidity` |
| 内外置标识 | `"外"` / `"内"` | `isOuter`（`isIsOuter`） |

### 5.3 点击 → 历史数据

点击光控卡片 → `openHistory(bean)` → 跳转 `LuxHistoryActivity`，携带：
- `STATION_ID` = `bean.id`
- `BOX_NAME` = `bean.name`

### 5.4 错误处理

| 错误接口 | 处理 |
|---------|------|
| `FAST_GROUP`（分组列表失败） | 隐藏 `cardFastGroup`、清空分组 adapter |
| `LUX_LIST`（光控列表失败） | 隐藏 `recyclerLux`、清空光控 adapter |

---

## 6. 光控实时数据（WebSocket）

> 代码中 `initData()` 的 WebSocket 连接与监听被注释，但 `handleSocketMsg` 逻辑保留（其它入口 `FastGroupChannelFragment` 等仍会连接 WebSocket）。iOS 端若需实时刷新可参照此协议。

`handleSocketMsg(text)`：

```kotlin
if (JsonDataUtil.getValue(text, "type") == "lux") {          // 消息 type == "lux"
    val bean = parse(text, LuxUpdataBean)                    // {luxId, lux, temp, hum, time}
    val pos = mPositionMap[bean.luxId]                       // 按 luxId 定位列表项
    if (pos != null && oldBean.id == bean.luxId) {
        oldBean.luxValue   = bean.lux
        oldBean.temperature = bean.temp.toInt()
        oldBean.humidity    = bean.hum.toInt()
        oldBean.fireTime    = bean.time
        mLuxAdapter.notifyItemChanged(pos)                   // 局部刷新
    }
}
```

`mPositionMap` 在光控列表回调里建立：`mPositionMap[bean.id] = index`。

---

## 7. 历史数据（LuxHistoryActivity）

### 7.1 入口参数

- `STATION_ID`（光控设备 id，代码里变量名 `mStationId`）
- `BOX_NAME`（设备名，显示为标题）

### 7.2 时间范围

- 初始：开始时间 = 当前时间减 2 小时（`Tu.subHour(2)`），结束时间 = 当前（`Tu.formatMinute()`）。
- 点「开始时间/截止时间」弹时间选择器（`ChooseTimeBottomDialog`）。
- 「查询」按钮校验：开始/截止时间都不能为空。

### 7.3 查询请求

```kotlin
getData() {
    jsonObject.put("stationId", stationId)
    jsonObject.put("start", startTime)
    jsonObject.put("end", endTime)
    jsonObject.put("index", 1)
    jsonObject.put("size", 20000)
    mViewModel.getLuxHistory(json)   // POST /device/lux/QueryLogs
}
```

### 7.4 历史列表显示（`LuxHistoryAdapter`，`list.reversed()` 倒序）

| 字段 | 显示 |
|------|------|
| 序号 | `position + 1` |
| 照度 | `"${luxValue} lux"` |
| 采集时间 | `fireTime` |
| 温度 | `"${temperature} ℃"` |
| 湿度 | `"${humidity} %"` |

### 7.5 报警颜色（卡片背景）

```kotlin
getAlarmColor(bean.luxValue < 100)   // luxValue < 100 → RGB(230,147,147) 报警红；否则白色
```

> 即照度低于 100 lux 的历史记录高亮显示（报警态）。

---

## 8. 一键开关灯分组列表与入口

### 8.1 分组列表显示（`FastGroupControlAdapter`）

`getFastGroupList()` → `POST /station/lux/QueryOutputGroup` → `FastLightGroupBean[]`（3 列网格，每项显示 `name`）。

### 8.2 交互

| 交互 | 行为 |
|------|------|
| 点击分组 | 权限 `FAST_CHECK` 校验后 → `FastControlDetailActivity`（`PAGE_TYPE=1` 通道列表） |
| 长按分组 | 权限 `FAST_DELETE` 校验后 → 确认框「是否要删除 <name> 控制分组?」→ `deleteFast(id)` |
| 「配置分组」按钮 | 权限 `FAST_CONTROL` 校验后 → `FastGroupManageActivity` |

### 8.3 删除分组

`deleteFast(groupId)` → `POST /station/lux/DeleteOutputGroup/ {id}` → 成功回调「删除成功」→ `onRefresh()`。

### 8.4 消息刷新

`busMsg`：收到 `Constant.RESULT`（`FastAddLightActivity`/`FastAddChannelActivity` 添加成功后发送）→ `onRefresh()`。

---

## 9. 分组配置（FastGroupManageActivity）

### 9.1 列表

复用 `getFastGroupList()`，`FastGroupManageAdapter` 每项显示：
- 分组名 `name`
- 「添加通道」按钮（`btnChannel`）
- 「添加单灯」按钮（`btnLight`）

### 9.2 新增分组

菜单「添加」→ `AddFastGroupDialogFrag`：

```kotlin
submit() {
    groupName = editName.trim()
    if (empty) toast("输入分组名称")
    else {
        FastAddBean{ id:"", name:groupName } → gson → addFast(json)
        // POST /station/lux/SaveOutputGroup
    }
}
```

成功 →「增加成功」→ dismiss → 回调刷新。

### 9.3 添加通道 / 添加单灯

点「添加通道」(type=1) /「添加单灯」(type=2) → `ChooseGroupDialogFragment`（选择配电箱分组）→ 选中后路由：

| type | 路由 action | 携带参数 |
|------|-----------|---------|
| 1 | `fastAddChannel` | `GROUP_ID`(选中分组 id)、`FAST_ID`(目标分组 id) |
| 2 | `fastAddLight` | `GROUP_ID`、`FAST_ID` |

### 9.4 点击分组 → 详情

点分组 item → `FastControlDetailActivity`（`PAGE_TYPE=2`）。

### 9.5 长按 → 删除

同 §8.3，`deleteFast(id)`。

---

## 10. 一键开关灯详情（FastControlDetailActivity + 通道/单灯）

### 10.1 入口参数

- `NAME`（分组名，显示为标题）
- `GROUP_ID`（分组 id）
- `PAGE_TYPE`（1/2）

### 10.2 Tab 结构（注意：当前实现只有 1 个 Tab）

- 用 `ViewPager2` + `TabLayout`，`TAB_FAST_GROUP_ARRAY = [通道, 单灯]`。
- `FastPagerAdapter.getItemCount()` 返回 **1**，`createFragment(0)` = `FastGroupChannelFragment`。
- 因此**当前实际只显示「通道」一个 Tab**，单灯 Tab 逻辑（`FastGroupLightFragment`）存在但 `getItemCount()=1` 未启用。

### 10.3 通道列表（`FastGroupChannelFragment`）

#### 数据

`getFastChannelDetail(groupId)` → `POST /station/lux/QueryChannels {id}` → `FastGroupChannelBean[]`。

#### 显示（`FastGroupControlChannelAdapter`）

| 字段 | 显示 |
|------|------|
| 站点名 | `stationName` |
| 通道 | `"K${channel}-${channelName}"` |
| 时间表 | `timeName` |
| 执行结果 | `state`（按状态着色） |

#### 一键开关灯操作（`PAGE_TYPE==1` 才显示按钮）

- 按钮：`btnFastOn`（开灯）、`btnFastOff`（关灯）、`tvChooseTime`（选择时间，默认当前 +1 小时）。
- 点击开/关：

```kotlin
confirmOperation(isOpen) {
    checkPermission(FAST_CONTROL)
    time = tvChooseTime.text
    title = "确定开灯/关灯 至 $time ?"
    确认后：
        idList = 所有项 paramId
        json = CommandUtil().newBoxOrderList(time, isOpen, idList)
        mCommandViewModel.sendNewCommand(SEND_OUTPUT_COMMAND, json)   // /station/command/SendOutputOld
}
```

#### WebSocket 结果回显

监听 `ORDER_CMD` + `cmdCode == handControl`：

```kotlin
cmdId → mResultMap[cmdId] → paramId → 定位 item → state = getResult(status)
```

- 初次命令返回（`newCommandViewModel`）：`mResultMap[bean.message(cmdId)] = bean.id(paramId)`，并先显示「正在执行...」或错误 message。

### 10.4 单灯列表（`FastGroupLightFragment`，当前未启用）

> 该 Fragment 完整实现但不在默认 Tab 中展示，iOS 端如需「单灯」Tab 可参考：

- 数据：`getFastLightDetail(groupId)` → `POST /station/lux/QueryLights {id}` → `FastGroupLightBean[]`。
- 显示（`FastGroupControlLightAdapter`）：`lightName`、`stationName`、`timeName`、`state`。
- 操作：开灯/关灯（`OperationSwitchSingleDialog`）、调光（`OperationSingleDimmerDialog`）。
- 命令：`controlLight(...)` 收集 `paramId` → `getNewCallJson` → `SEND_LAMP_COMMAND`。

---

## 11. 添加通道到分组（FastAddChannelActivity）

### 11.1 入口参数

- `GROUP_ID`（选中配电箱分组 id，用于拉取通道列表）
- `FAST_ID`（目标一键开关灯分组 id，用于提交）

### 11.2 数据

`getGroupControlList(groupId)` → `POST /station/config/QueryOutput/ {groupId, stationId:0}` → `NewGroupControlBean.DataData[]`（字段：`id`、`name`、`timeName`）。

### 11.3 筛选

- 「选择通道」：`AlertDialogFactory` 多选通道名 → 过滤 `mGroupBeans`。
- 「选择时间表」：多选时间表名 → 过滤。
- 「全选」CheckBox：选中/取消全部。

### 11.4 提交

```kotlin
submit() {
    if (mSelectedList.isEmpty()) toast("请选择要添加的通道")
    else {
        channelList = mSelectedList.map { it.id }
        addChannel(groupId /*FAST_ID*/, channelList)
        // POST /station/lux/SetChannels {id, channels:[ids]}
    }
}
```

成功 →「通道添加成功」→ `addFastResult()`：`sendMsg(RESULT, ...)` + finish。

---

## 12. 添加单灯到分组（FastAddLightActivity）

### 12.1 入口参数

- `GROUP_ID`（选中配电箱分组 id）
- `FAST_ID`（目标一键开关灯分组 id）

### 12.2 数据

`getGroupLightList(groupId)` → `POST /station/light/Simple {stationId:0, groupId}` → `GroupLightBean.DataBean[]`（字段：`id`、`name`、`deviceId`、`timeId`、`timeName`、`stationId`、`stationName`）。

### 12.3 筛选

- 「选择时间表」多选时间表名 → 过滤（`selectTimeMap`）。
- 「全选」CheckBox。

### 12.4 提交

```kotlin
submit() {
    if (mSelectedList.isEmpty()) toast("请选择要添加的单灯")
    else {
        lightIdList = mSelectedList.map { it.id }
        addLight(groupId /*FAST_ID*/, lightIdList)
        // POST /station/lux/SetLights {id, lights:[ids]}
    }
}
```

成功 →「单灯添加成功」→ `addFastResult()`。

---

## 13. 命令执行与状态回显

### 13.1 一键开关灯（通道）

- 接口：`POST /station/command/SendOutputOld`。
- 请求体由 `CommandUtil().newBoxOrderList(time, isOpen, idList)` 生成（含目标时间、开/关、通道 id 列表）。
- 结果通过两条链路回显：
  1. `sendNewCommand` 同步返回（`newCommandViewModel`）→ 建立 `cmdId → paramId` 映射，先显示「正在执行...」/错误。
  2. WebSocket 推送 `type=cmd` + `cmdCode=handControl` + `status` → `getResult(status)` 更新该项 `state`。

### 13.2 单灯（`FastGroupLightFragment`，未启用 Tab）

- 接口：`POST /station/command/SendLampOld`（`getNewCallJson`）。
- 同样 WebSocket 回显 `state`。

### 13.3 状态文案（`OrderUtil.getResult`）

| status | 文案 |
|--------|------|
| 2 | 指令已发送 |
| 4 | 执行中 |
| 5 | 已重发 |
| 7 | 指令超时 |
| 8 | 执行失败 |
| 9 | 执行成功 |

---

## 14. 数据结构 JSON 明细

> 字段名即服务端反序列化 key，iOS 端务必一致。

### 14.1 `LuxListBean`（光控列表）

```json
{
  "id": "", "ruleId": 0, "ruleName": "", "name": "",
  "transType": 0, "transTypeName": "", "code": "",
  "smallType": 0, "smallTypeName": "", "transChannel": 0,
  "address": 0, "buildDate": "", "location": "", "simCard": "",
  "fireTime": "", "luxValue": 0, "temperature": 0, "humidity": 0,
  "isOuter": false
}
```

### 14.2 `LuxUpdataBean`（WebSocket 光控实时数据）

```json
{ "type": "lux", "appType": "", "customerId": 0, "luxId": "", "lux": 0, "temp": 0.0, "hum": 0.0, "time": "" }
```

### 14.3 `LuxHistoryBean`（光控历史）

```json
{ "count": 0,
  "list": [ { "luxId": "", "fireTime": "", "luxValue": 0, "temperature": 0, "humidity": 0 } ] }
```

### 14.4 `FastLightGroupBean`（一键开关灯分组）

```json
{ "id": "", "customerId": 0, "appType": "", "name": "" }
```

### 14.5 `FastGroupChannelBean`（分组内通道）

```json
{ "channelName": "", "stationName": "", "channel": 0, "timeName": "",
  "deviceId": 0, "paramId": 0, "id": "", "state": "" }
```

### 14.6 `FastGroupLightBean`（分组内单灯）

```json
{ "lightName": "", "stationName": "", "timeName": "", "deviceId": 0, "paramId": 0, "id": "", "state": "" }
```

### 14.7 `FastAddBean`（新增分组 / 添加通道 / 添加单灯请求体）

```json
{ "id": "", "name": "", "channels": [1,2], "lights": [3,4], "remark": "" }
```

- 新增分组：只用 `id`（空串）+ `name`。
- 添加通道：`id`（分组 id）+ `channels`。
- 添加单灯：`id`（分组 id）+ `lights`。

### 14.8 `GroupLightBean.DataBean`（添加单灯页数据源）

```json
{ "id": 0, "name": "", "deviceId": 0, "timeId": 0, "timeName": "", "stationId": 0, "stationName": "" }
```

### 14.9 `NewGroupControlBean.DataData`（添加通道页数据源，仅关键字段）

```json
{ "id": 0, "name": "", "timeName": "", "deviceId": 0, "..." : "..." }
```

---

## 15. iOS 实现对照清单

1. **主页面**：光控网格（2 列）+ 一键开关灯分组网格（3 列）+「配置分组」按钮。
2. **光控面板**：`QueryState` 列表 → 显示名称/照度/时间/温度/湿度/内外置；点击进历史。
3. **历史数据**：`QueryLogs`（`stationId/start/end/index/size`）→ 倒序列表 + `luxValue<100` 高亮。
4. **分组列表**：`QueryOutputGroup`；点击（`PAGE_TYPE=1`）进详情、长按删除、配置按钮进管理。
5. **分组管理**：新增分组（`SaveOutputGroup`）、删除（`DeleteOutputGroup`）、添加通道/单灯（先选配电箱分组 → 路由添加页）。
6. **详情**：通道 Tab（`QueryChannels`）+ 一键开/关灯（`SendOutputOld`）+ 时间选择 + 状态回显。
7. **添加通道**：`QueryOutput` 列表 + 通道/时间表筛选 → `SetChannels`。
8. **添加单灯**：`light/Simple` 列表 + 时间表筛选 → `SetLights`。
9. **权限校验**：`FAST_CONTROL`/`FAST_CHECK`/`FAST_DELETE` 三个码控制按钮可见/可操作。
10. **命令状态**：`getResult(status)` 文案 + 颜色映射。

### 文案对照（关键）

| 场景 | 文案 |
|------|------|
| 添加分组-空名 | 输入分组名称 |
| 添加分组-成功 | 增加成功 |
| 添加分组-失败 | 操作失败，请重试！ |
| 删除分组确认 | 是否要删除 <name> 控制分组? |
| 删除成功 | 删除成功 |
| 添加通道-成功 | 通道添加成功 |
| 添加单灯-成功 | 单灯添加成功 |
| 添加通道-未选 | 请选择要添加的通道 |
| 添加单灯-未选 | 请选择要添加的单灯 |
| 历史-未选开始 | 请选择开始时间 |
| 历史-未选截止 | 请选择截止时间 |
| 选择分组 | 选择分组 |
| 开灯/关灯确认 | 确定开灯/关灯 至 <time> ? |

---

## 16. 注意事项与易踩坑点

1. **`PAGE_TYPE` 决定按钮显隐**：`=1`（主页进入）显示一键开/关灯按钮；`=2`（管理页进入）隐藏操作按钮（只查看）。
2. **FastControlDetailActivity 当前只有「通道」Tab**：`FastPagerAdapter.getItemCount()=1`，单灯 Tab 虽实现但未启用；iOS 若需单灯 Tab 需自行扩展。
3. **`GROUP_ID` 与 `FAST_ID` 语义不同**：添加通道/单灯页里 `GROUP_ID` 是「配电箱分组 id」（拉列表数据），`FAST_ID` 是「一键开关灯分组 id」（提交目标）。
4. **历史数据倒序**：`hisBean.list.reversed()` 显示最新在前。
5. **历史报警阈值**：`luxValue < 100` 判定报警高亮。
6. **光控实时**：`type=="lux"` 的 WebSocket 消息，字段为 `lux`（int）、`temp`/`hum`（double，显示时 `toInt`）。
7. **删除接口路径**：`DeleteOutputGroup/` 末尾带斜杠。
8. **命令状态双重回显**：先由 `sendNewCommand` 返回建立 `cmdId↔paramId` 映射并显示「正在执行...」，再由 WebSocket 推送最终 `status` 覆盖。
9. **`id` 类型混杂**：`LuxListBean.id` 为 String（用作历史查询的 stationId），`FastGroupChannelBean.paramId` 为 int（命令用），`FastGroupLightBean` 的 `id` 在运行时被 UUID 覆盖（用于定位）。
10. **温度/湿度单位**：温度 `℃`，湿度 `%`，照度 `lux`。
11. **查询历史 size 固定 20000**，index 固定 1（一次拉全量）。
12. **权限**：iOS 需对齐「无权限不显示/不响应」的逻辑（`FAST_CONTROL`/`FAST_CHECK`/`FAST_DELETE`）。

---
