# 状态操作 Tab 开发指导文档

## 1. 模块总览与页面地图

```text
首页主 Tab —— 「状态操作」(TabStateFragment，标题：状态操作)
│
├─ 子 Tab 0：配电箱 (NewNodeFragment)          —— 分组/站点 树形列表
│    ├─ 点击站点(常规配电箱)  ────────────────→  配电箱详情 PowerBoxActivity
│    │        └─ 右上角菜单「单灯」 ─────────→  单灯详情 SingleDetailActivity
│    ├─ 点击站点(纯单灯站点 isLight && !isPower) → 单灯详情 SingleDetailActivity
│    ├─ 点击站点(水浸站 stationType=4) ───────→  水浸箱页面 NewWaterBoxActivity（不在本文档范围）
│    └─ 点击分组行 / 分组控制按钮 ────────────→  展开折叠 / 群组控制 NewGroupControlActivity
│
├─ 子 Tab 1：单灯 (NodeLampCountFragment)      —— 分组/站点 树形列表（带灯数）
│    └─ 点击站点 ────────────────────────────→  单灯详情 SingleDetailActivity
│
└─ 子 Tab 2：智控 (LuxListFragment)            —— 仅道路版存在，不在本文档范围
```

App 类型决定子 Tab 数量：

| App 类型 | 本地 key | 子 Tab |
|---|---|---|
| 道路版 `road` | `Apptype` | 配电箱、单灯、智控 |
| 工厂版 `factory` | `Apptype` | 配电箱、单灯 |

工厂版下点击站点不走本文档的两个详情页，而是路由到「工厂配电箱」页面（`factoryBox`）。

---

## 2. 通用约定（所有页面共用）

### 2.1 网络接口约定

- 除个别 GET 外，全部接口为 `POST + JSON body`。
- 响应统一结构：`{ "code": 0, "data": ... }`，业务数据在 `data` 中。
- 服务地址由 `WebServiceConfig` 按环境标志（`envFlag`）提供根地址；iOS/uni-app 侧需要与后端确认同一套环境配置。
- WebSocket 地址 = 根地址 + `api/center/data`，带心跳与断线重连（最大重连间隔 30s，指数退避）。

### 2.2 权限约定

权限判断统一为：本地缓存的用户操作码集合中是否包含对应 code。无权限时 toast「没有相关权限」。

| code | 含义 | 使用位置 |
|---|---|---|
| `ga` | 添加分组 | 配电箱页长按分组的菜单入口 |
| `gd` | 删除分组 | 分组管理菜单 → 删除分组 |
| `sa` | 添加站点 | 配电箱页长按站点的菜单入口 |
| `sd` | 删除站点 | 站点管理菜单 → 删除站点 |
| `dco` | 设备操作权限 | 单灯页底部操作按钮区、开关灯按钮、改名、时钟操作等 |

### 2.3 设备状态语义（重要，全模块通用）

每个设备/站点都有三个布尔字段：`online`（在线）、`running`（运行/亮灯）、`alarm`（报警）。

- `online = false` → 离线。
- `online = true && running = true` → 亮灯（运行）状态。
- `online = true && running = false` → 关灯（停止）状态。
- `alarm = true` → 该设备当前带报警。

单灯五态图标判定（单灯列表、单灯 Tab 树、单灯详情页通用）：

| 条件（按顺序判断） | 图标资源名 | 含义 |
|---|---|---|
| `!online` | `single_line_off_tile` | 离线 |
| `online && running && alarm` | `single_on_alarm` | 亮灯 + 报警 |
| `online && running` | `single_on` | 亮灯 |
| `online && !running && alarm` | `single_tile_off_alarm2` | 关灯 + 报警 |
| `online && !running` | `single_off_alarm` | 关灯 |

### 2.4 颜色约定

| 名称 | 色值 | 用途 |
|---|---|---|
| `mainTone` | `#021629` | 主文字色（深蓝黑），站点/单灯名称 |
| `mainTone1` | `#68737D` | 次级文字色（灰），提示、时间 |
| `colorAccent` | `#3880FC` | 强调色（蓝），数量、选中图标 |
| `colorRed` | `#FF0000` | 报警红（灭灯报警文字） |
| `pageBg` | `#EFF3FB` | 详情页背景 |
| `colorWhite` | `#FFFFFF` | 列表页背景、卡片背景 |

### 2.5 本地存储项（uni-app 用 storage 对应）

| key | 含义 | 使用页面 |
|---|---|---|
| `Apptype` | App 类型 `road` / `factory` | Tab 数量、站点点击路由 |
| `root_tree` | 左树根节点名称 | 两个树页面左上角标题 |
| `show_pole` | 单灯名称是否带灯杆名前缀 | 单灯详情页 |
| `show_power` | 单灯是否显示功率/亮度列 | 单灯详情页 |

### 2.6 WebSocket 消息约定

消息为 JSON 字符串，用 `type` 字段区分：

| type | 含义 | 关键字段 |
|---|---|---|
| `cmd` | 指令执行结果回执 | `cmdCode`（指令代码）、`commandId`（与下发接口返回的 cmdId 对应）、`status`、`content` |
| `data` | 设备采集数据更新 | `paramId`（设备属性 id）、`data`（新的 lastData JSON） |
| `state` | 设备状态变化 | `id`、`isIsParam`（是否属性）、`isIsPole`（是否灯杆）、`isOnline`、`isRunning`、`isAlarm` |

指令执行状态码 `status`：

| 值 | 文案 |
|---|---|
| 2 | 指令已发送 |
| 4 | 执行中 |
| 5 | 已重发 |
| 7 | 指令超时 |
| 8 | 执行失败 |
| 9 | 执行成功 |

结果匹配方式：下发指令接口返回的 `message` 字段即 `cmdId`；WebSocket 回执里的 `commandId` 与之相等时，才是本页面发出指令的结果。

---

## 3. 状态操作 Tab 容器（TabStateFragment）

- 页面标题栏文案：`状态操作`。
- 结构：顶部 `TabLayout` + 可左右滑动的 `ViewPager`。
- Tab 文案：道路版 `配电箱 / 单灯 / 智控`；工厂版 `配电箱 / 单灯`。
- 每次进入页面（onResume）都会重新读取 `root_tree` 并重建子页面观察者；子页面均支持下拉刷新。

uni-app 实现建议：一个页面内用 `swiper` + 自定义 tab 头即可，`Apptype` 决定 tab 项。

---

## 4. 配电箱 Tab（NewNodeFragment）

### 4.1 页面结构

```text
┌────────────────────────────────────────────┐
│ (树图标) 根节点名称(粗体15sp)    [🔍 搜索] │  ← 顶部区
├────────────────────────────────────────────┤
│ ▸ 分组 A                        [群组控制] │
│   ▸ 分组 A-1                    [群组控制] │  ← 树形列表
│     (配电箱图标) 站点名                     │     可下拉刷新
│ ...                                        │
└────────────────────────────────────────────┘
```

- 左上角：根图标（`node_city`）+ 根节点名称，名称来自本地 `root_tree`，粗体 15sp 黑色。
- 右上角：搜索框（圆角背景 `shape_search_edit_bg`，宽 60dp 高 28dp，放大镜图标 14dp + 文案「搜索」13sp）。
- 列表：垂直树形列表，行高约 48dp；行缩进 = `16dp + min(level, 5) × 18dp`。
- 整页支持下拉刷新：清空列表后重新请求分组 + 站点。

### 4.2 数据加载流程

1. `POST /station/config/QueryGroup`（无 body）→ 分组列表。
2. 分组返回后 `POST /station/config/QueryStation`，body：`{"type": 0}` → 站点列表。
3. 在后台线程按 4.4 算法构建树，回到主线程渲染。

### 4.3 接口字段

分组 `NodeGroupBean.DataData`：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | int | 分组 id |
| `name` | string | 分组名 |
| `parentId` | int | 父分组 id，`0` 表示顶层 |
| `sort` | int | 排序 |
| `showFlag` | int | 显示标志（本页面未使用） |

站点 `NewNodeBoxBean.DataBean`：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | int | 站点 id（后续页面都用它） |
| `name` | string | 站点名 |
| `groupId` | int | 所属分组 id |
| `stationType` | int | 站点类型：`1` 配电箱、`2` 隧道、`3` 单灯站点、`4` 水浸站、`5` 太阳能 |
| `supplyMode` | int | 供电方式：`1` 市电、`2` 专变 |
| `type` | int | 箱类型（本页面仅透传） |
| `online` / `running` / `alarm` | bool | 状态三要素，见 2.3 |
| `hasPower` | bool | 是否配置了配电（有总配电等设备） |
| `hasLight` | bool | 是否有单灯 |
| `light` | int | 单灯数量（单灯 Tab 用） |
| `lat` / `lng` | double | 坐标 |
| `refreshTime` | string | 最近刷新时间 |
| `lowFlow` / `lowStart` / `lowEnd` | - | 下半夜调光相关（本页面仅透传） |
| `location` | string | 安装位置描述 |

### 4.4 树构建算法（必须照抄的逻辑）

```text
输入：groupList（分组）, nodeList（站点）

1. 为每个分组建立节点 GroupNode{ id, name, parentId, isVisible=true, isExpand=false }
2. 孤儿分组处理：parentId != 0 但在分组表中找不到父分组 → 强制 parentId = 0（作为顶层）
3. 挂载子分组：parentId != 0 → 加入父分组的 children
4. 挂载站点：按 groupId 找到所属分组，生成 SiteNode 加入该分组 children
   （找不到所属分组的站点直接丢弃）
5. 顶层 = 所有 parentId == 0 的分组，永远可见
6. 递归展平（默认展开层级 defaultExpandLevel = 1）：
   - 节点层级 = 当前递归深度（顶层为 0）
   - 分组的展开状态 = (层级 < 1)，即只有第 0 层分组默认展开
   - 第 0 层永远显示；其余节点可见性 = 父节点可见 && 父节点展开
7. 输出「平铺 + 可见性标记」的列表；渲染时只显示可见项
```

展开/折叠交互：点击分组行 → 切换该分组 `isExpand` → 递归刷新所有后代的可见性（父折叠则后代全部隐藏；父展开时子分组是否显示其后代取决于子分组自身的 `isExpand`）→ 重新过滤渲染。

### 4.5 行样式

分组行：

- 图标：展开 `node_city`，折叠 `node_group`（22×24dp）。
- 名称：14sp 粗体 `mainTone`。
- 右侧按钮「群组控制」（白字、蓝底圆角），点击 → `NewGroupControlActivity`，参数 `groupId`。

站点行：

- 图标：按 4.6 判定（22×24dp）。
- 名称：14sp 粗体 `mainTone`。
- 无右侧按钮。

### 4.6 站点图标判定（NodeIconUtil，按顺序匹配）

| 条件                                             | 在线且运行                                           | 在线未运行                                               | 离线                     |
|------------------------------------------------|-------------------------------------------------|-----------------------------------------------------|------------------------|
| `stationType=4 && supplyMode=1`（水浸站）           | 报警 `water_map_alarm` / 正常 `water_map_normal`    | 同左（按报警与否）                                           | `water_map_offline`    |
| `stationType=3 && hasLight && !hasPower`（纯单灯站） | 报警 `single_on_alarm` / 正常 `single_on`           | 报警 `single_tile_off_alarm2` / 正常 `single_off_alarm` | `single_line_off_tile` |
| `stationType=1 && supplyMode=1`（普通配电箱）         | 报警 `box_on_alarm_new` / 正常 `box_on_new`         | 报警 `box_off_alarm_new` / 正常 `box_off_new`           | `box_offline_new`      |
| `stationType=2`（隧道）                            | 报警 `tunnel_on_alarm` / 正常 `tunnel_on`           | 报警 `tunnel_off_alarm` / 正常 `tunnel_off`             | `tunnel_offline`       |
| `stationType=1 && supplyMode=2`（专变配电箱）         | 报警 `specialized_on_alarm` / 正常 `specialized_on` | 报警 `specialized_off_alarm` / 正常 `specialized_off`   | `specialized_offline`  |
| `stationType=5`（太阳能）                           | 报警 `solar_on_alarm` / 正常 `solar_on`             | 报警 `solar_off_alarm` / 正常 `solar_off`               | `solar_offline`        |
| 其它                                             | -                                               | -                                                   | `box_offline_new`      |

### 4.7 点击跳转规则（道路版）

| 条件（按顺序判断）               | 目标页面                      | 传递参数                                      |
|-------------------------|---------------------------|-------------------------------------------|
| `stationType == 4`      | 水浸箱页面                     | `stationId`、`boxName`                     |
| `hasLight && !hasPower` | 单灯详情 SingleDetailActivity | `stationId`、`boxName`、`parentId`(所属分组 id) |
| 其它                      | 配电箱详情 PowerBoxActivity    | `stationId`、`boxName`                     |

工厂版：统一路由到工厂配电箱页（参数 `stationId`、`boxName`）。

### 4.8 长按管理菜单

长按分组（需要 `ga` 权限，否则 toast 无权限）：

| 菜单项  | 行为                                                                      |
|------|-------------------------------------------------------------------------|
| 添加分组 | 在该分组下新增子分组弹窗                                                            |
| 编辑分组 | 改名等编辑弹窗                                                                 |
| 移动分组 | 移动到其他分组                                                                 |
| 删除分组 | 需 `gd` 权限；确认框「确定要删除当前分组?」→ `POST /sys/auth/DeleteGroupOld {"id": 分组id}` |
| 添加站点 | 在该分组下新增站点弹窗                                                             |

长按站点（需要 `sa` 权限）：

| 菜单项  | 行为                                                                              |
|------|---------------------------------------------------------------------------------|
| 编辑站点 | 编辑站点弹窗                                                                          |
| 移动站点 | 移动到其他分组                                                                         |
| 删除站点 | 需 `sd` 权限；确认框「确定要删除当前站点?」→ `POST /station/config/DeleteStation {"list":[站点id]}` |
| 编辑位置 | 打开地图选点页（参数：类型=配电箱、站点 id、lat、lng、站点名），保存后返回                                      |
| 克隆站点 | 复制站点弹窗 → `POST /station/config/CopyStation`                                     |

长按左上角根节点名称 → 弹出「添加顶级分组」。

### 4.9 搜索

点击右上角搜索 → 搜索弹窗（类型 1）：

1. 输入名称（必填，空则提示「输入配电箱名称」）。
2. `POST /station/config/FilterTree`，body：`{"filter": "关键字"}`。
3. 结果过滤：`isStation == true` 且名称包含关键字。
4. 点击结果项 → 直接进配电箱详情页（参数 `stationId`、`boxName`）。

---

## 5. 单灯 Tab（NodeLampCountFragment）

复用配电箱 Tab 的布局文件，行为差异如下。

### 5.1 与配电箱 Tab 的差异对照

| 项目     | 配电箱 Tab     | 单灯 Tab                                          |
|--------|-------------|-------------------------------------------------|
| 默认展开   | 只展开第 0 层    | 所有分组全部展开                                        |
| 站点过滤   | 显示全部站点      | 只显示 `hasLight == true` 或 `stationType == 3` 的站点 |
| 夜间灭灯报警 | 无           | 当前时间 ≥ 当天 17:00 时先查灭灯报警（见 5.2）                  |
| 站点行右侧  | 无           | 「{light} 盏」灯数                                   |
| 站点图标   | 全类型图标表（4.6） | 单灯五态图标（2.3）                                     |
| 点击站点   | 按类型分流       | 一律进单灯详情页                                        |
| 搜索类型   | 1 → 配电箱详情   | 3 → 单灯详情                                        |

### 5.2 夜间灭灯报警逻辑

进入页面/刷新时：

```text
if 当前时间 >= 当天17:00:
    1. POST /station/alarm/QueryLightDetail
       body: { groupId:0, stationId:0, name:"", type:23, level:-1,
               start, end, size:100, index:1 }
       start/end 规则：
         0~8 点   → 当天 00:00:00 ~ 当天 08:00:00
         其它时间 → 当天 17:00:00 ~ 次日 00:00:00
    2. 报警列表返回后再请求分组/站点
else:
    直接请求分组/站点
```

> 注：源码注释写的是「17:00 ~ 次日 08:00」，实际实现只判断「当前时间 ≥ 当天 17:00」，复刻时以实际行为为准（或与产品确认）。

站点灭灯报警标记：`isLampAlarm = 报警列表中存在 stationId == 站点id 的记录`。

### 5.3 站点行显示

- 图标：单灯五态（2.3 表）。
- 名称：14sp 粗体，默认 `mainTone`；`isLampAlarm == true` 时变 `colorRed`。
- 名称右侧：`isLampAlarm` 时显示红色圆点（12dp 圆形）。
- 行尾：`{light} 盏`，`colorAccent` 14sp 粗体。
- 分组行：与配电箱 Tab 相同（无「群组控制」按钮）。

点击站点 → 单灯详情，参数：`stationId`、`parentId`(分组 id)、`boxName`。

---

## 6. 配电箱详情页（PowerBoxActivity）

### 6.1 入口参数

| 参数 | 必填 | 说明 |
|---|---|---|
| `stationId` | 是 | 站点 id |
| `boxName` | 是 | 站点名（标题显示） |

### 6.2 页面结构

```text
┌────────────────────────────────────────────┐
│ ← │ 站点名称(标题)                 │ ⋮菜单 │
├────────────────────────────────────────────┤
│        最近召测时间(12sp 灰, 居中)          │
├────────────────────────────────────────────┤
│ 设备分区列表（可下拉刷新，竖向滚动）         │
│  · 总配电（竖向卡片）                       │
│  · 漏电 / 转换开关 / 柜门 / 门锁 /          │
│    烟雾 / 水浸 / 控制输出 / 接触器 / 支路    │
│ —— 无任何设备时显示空态页 ——                │
├────────────────────────────────────────────┤
│ 召测 │ 查询时钟 │ 校准时钟 │ 导航           │  ← 底部操作栏
└────────────────────────────────────────────┘
```

- 标题：站点名，长度 > 14 字自动缩小（16sp → 13sp）。
- 最近召测时间：总配电 `fireTime` 不为 0 时显示，格式 `yyyy-MM-dd HH:mm:ss`。
- 空态页：空图片 + 「该站点没有设备」+ 两个按钮「扫码添加」「手动添加」。

### 6.3 数据加载顺序

1. `POST /station/analyse/EnergyTotal`，body：`{"groupId":0, "stationId": 站点id}` → `data.today` 当日能耗（kWh）。
2. 成功后 `POST /station/config/QueryPowerBox/`，body：`{"groupId":0, "stationId": 站点id}` → 配电箱完整设备详情。
   - 若第 1 步出错，跳过能耗直接请求第 2 步。
   - enp为true则总配电显示完整内容否则总配电模块只显示ABC相电压和内外控
3. 建立 WebSocket 连接并监听回执。

### 6.4 详情响应结构（StationDetailBean.DataBean）

| 字段 | 类型 | 说明 |
|---|---|---|
| `mains` | 数组 | 总配电 |
| `switchs` | 数组 | 转换开关 |
| `outputs` | 数组 | 控制输出通道 |
| `contacts` | 数组 | 接触器 |
| `branchs` | 数组 | 支路配电（也用于漏电区块） |
| `doors` | 数组 | 柜门 |
| `locks` | 数组 | 门锁 |
| `smokes` | 数组 | 烟雾 |
| `waters` | 数组 | 水浸 |
| `leakage` / `voltages` | 数组 | 预留（本页未直接使用） |
| `deviceIds` | 数组 | 设备 id 集合 |
| `lat` / `lng` | double | 站点坐标（导航用） |

每个设备对象通用结构：

```json
{
  "id": 123, "name": "设备名", "stationId": 1, "deviceId": 9,
  "content": { ... },   // 配置参数（额定值、阈值、使能位等）
  "lastData": { "time": 0, ... },  // 最近一次采集数据；time==0 表示从未采到数据
  "extraData": { ... }, // 扩展数据
  "fireTime": 1690000000, "updateTime": "..."
}
```

### 6.5 分区组装顺序（自上而下，只渲染有数据的分区）

| 顺序 | 分区 | 数据条件 | 内部排列 |
|---|---|---|---|
| 1 | 总配电 | `mains` 非空 | 竖向 |
| 2 | 漏电 | `branchs` 中 `!content.enp && content.enl` 非空 | 竖向 |
| 3 | 转换开关 | `switchs` 非空（按 id 升序） | 横向 |
| 4 | 柜门 | `doors` 非空 | 横向 |
| 5 | 门锁 | `locks` 非空 | 横向 |
| 6 | 烟雾 | `smokes` 非空 | 横向 |
| 7 | 水浸 | `waters` 非空 | 横向 |
| 8 | 控制输出 | `outputs` 非空 | 横向，顶部含「控制所有通道」勾选 |
| 9 | 接触器 | `contacts` 非空 | 横向 |
| 10 | 支路配电 | `branchs` 非空（且存在接触器时） | 竖向 |

普通分区卡片头部：分区标题 + 设备数量；控制输出/总配电/支路/漏电为独立卡片样式。

> 注意：支路初始列表的过滤条件用的是**未过滤** contacts 列表的第 0 个（`contacts[0].id`），而接触器列表本身是按第一个输出通道过滤的。复刻时建议统一为「第一个显示接触器的 id」，行为更合理且与点击联动一致。

### 6.6 总配电区块（MainBoxAdapter）

按 `content.enp` 分两种行：`true` → 完整总配电行；`false` → 仅电压行和内外控状态。

完整总配电行字段：

| 显示项 | 数据来源 | 备注 |
|---|---|---|
| 名称 | `name` | |
| 通讯状态 | 固定文案「在线」 | |
| 更新时间 | `updateTime` | |
| 电表计数 | `lastData.e` | |
| 峰值负载 | `extraData.hpv` | 可能为空 |
| 当日能耗 | 第 1 步接口的 `today` | |
| A/B/C 相电压 | `lastData.ua/ub/uc` | 越界变报警色，见下 |
| A/B/C 相电流 | `lastData.ca/cb/cc` | |
| 漏电电流 | `lastData.cl` | |
| 电池电压 | `lastData.battery` | |
| A/B/C 额定电流 | `content.car/cbr/ccr` | |
| A/B/C 额定容量 | `content.par/pbr/pcr` | |
| A/B/C 功率因数 | `lastData.fa/fb/fc` | |
| A/B/C 无功功率 | 计算值：`ua×ca − pa`（B/C 同理） | |
| A/B/C 有功功率 | `lastData.pa/pb/pc` | |
| 经/纬度 | `lng` / `lat` | 保留 3 位小数 |
| 内外控状态 | 见下 | |

三相电压颜色判定（A 相示例，B/C 同理）：

```text
下限 = content.uar × content.ual ÷ 100
上限 = content.uar × content.uah ÷ 100
下限 ≤ ua ≤ 上限 → 正常色；否则报警色
（B 相用 ubr/ubl/ubh，C 相用 ucr/ucl/uch，结果保留 1 位小数）
```

内外控判定：

```text
content.ctrlc > 0 且 lastData.ctrl >= 0：
    lastData.ctrl == content.ctrlv → 「内控」
    否则 → 「外控」
其余情况 → 「默认」
```

电压行（无电表）：名称、三相电压、内外控状态。

### 6.7 漏电区块（BoxLeakAdapter）

数据源：`branchs.filter(!content.enp && content.enl)`。

| 显示项 | 来源 |
|---|---|
| 名称 | `name` |
| 漏电值 | `lastData.cl` |
| 轻微阈值 | `content.ll` |
| 严重阈值 | `content.lh` |
| 脱扣值 | `content.lx` |

判定：`cl ≥ lh` → 「严重漏电」；`ll ≤ cl < lh` → 「轻微漏电」；`cl < ll` → 「正常」。

### 6.8 转换开关（BoxTransferAdapter）

每行：名称 + 状态图标。状态 = `lastData.v`：

| v | 文案 | 图标 |
|---|---|---|
| 0 | 停止 | `switch_stop` |
| 1 | 手动 | `switch_handle` |
| 2 | 遥控 | `switch_auto` |
| 3 | 时控 | `switch_time` |
| 其它 | - | `switch_auto` |

### 6.9 柜门 / 门锁 / 烟雾 / 水浸

通用规则：`lastData.time == 0`（从未采到数据）→ 显示默认正常态。

柜门（`content.sc > 0` 才显示状态）：

| 条件 | 文案 | 样式 |
|---|---|---|
| `time == 0` | 闭合 | 正常底色 + `door_close` |
| `av != sv` | 闭合 | 正常底色 + `door_close` |
| `av == sv` | 开 | 报警底色 + `door_open` |

门锁：

| 条件 | 文案 | 样式 |
|---|---|---|
| `time == 0` | 闭合 | 正常底色 + `lock_normal` |
| `av == 1` | 闭合 | 正常底色 + `lock_normal` |
| 其它 | 打开 | 报警底色 + `lock_open` |

烟雾：

| 条件 | 文案 | 样式 |
|---|---|---|
| `time == 0` | 正常 | 正常底色 + `smoke_normal` |
| `sv == 1` | 正常 | 正常底色 + `smoke_normal` |
| 其它 | 报警 | 报警底色 + `smoke_alarm` |

水浸：

| 条件 | 文案 | 样式 |
|---|---|---|
| `time == 0` | 正常 | 正常底色 + `water_normal` |
| `s1==a1 或 s2==a2 或 s3==a3` | 报警 | 报警底色 + `water_alarm` |
| 其它 | 正常 | 正常底色 + `water_normal` |

### 6.10 控制输出区块（BoxOutputAdapter）

每个输出通道一行（横向排列）：

- 名称：`"K{content.oc} {name}"`（`oc` 为通道序号）。
- 时间表摘要：由 `timeContent` 解析（如 `19:27 - 开启, 05:25 - 关闭` 形式）。
- 状态图标：`lastData.ov == 1` → `switch_on`（开），否则 `switch_off`（关）。
- 「开灯」「关灯」两个按钮：
  - 仅当用户有 `dco` 操作权限时显示；
  - 仅当前行被选中时可点击；
  - 点击 → 6.14 开关灯流程。
- 选中态：行背景切换（选中 `shape_output_select_bg` / 未选 `shape_output_normal_bg`）；默认选中第一个。
- 点击行 → 选中该行，并联动刷新接触器、支路（见 6.13）。
- 区块顶部「控制所有通道」勾选框：勾选后，开关灯指令发送到**全部输出通道**而不是当前通道。

### 6.11 接触器区块（BoxContractorAdapter）

- 名称 + 状态文字 + 状态图标。
- 状态 = `lastData.sv`：`1` 闭合 / `0` 断开 / `-1` 未测量 / `-2` 未配置。
- 图标：`sv == 1` → `contactor_on` 正常底色；否则 `contactor_off` 报警底色。
- 默认选中第一个；点击选中并联动刷新支路列表。

### 6.12 支路区块（BoxBranchAdapter）

每行字段：

| 显示项 | 规则 |
|---|---|
| 名称 | `name` |
| A/B/C 相电流 | `{lastData.ca}[{content.car}]`；`ca == -1` → 「未测量」；`-2` → 「未配置」 |
| 漏电 | `lastData.cl`：`-1` 未测量 / `-2` 未配置 / `0` 「无漏电」/ 其它显示数值 |
| 时间表状态 | `lastData.tv`：`-1` 未设置 / `-2` 已删除 / `0` 灭灯时段 / `1` 亮灯时段 / `2` 未启用 |
| 总/分标志 | `content.isTt`：`true` → 「总」（主色底）；`false` → 「分」（次色底） |

### 6.13 输出 → 接触器 → 支路 三级联动

```text
初始状态：
  接触器列表 = contacts.filter(content.oid == outputs[0].id)，默认选中第一个
  支路列表   = branchs.filter(content.cid == 初始接触器id)

点击某个输出通道：
  接触器列表 = contacts.filter(content.oid == 该输出.id)，默认选中第一个
  支路列表   = branchs.filter(content.cid == 第一个接触器.id)，按 content.isTt 降序

点击某个接触器：
  支路列表 = branchs.filter(content.cid == 该接触器.id)，按 content.isTt 降序
```

若 `branchs` 为空：只显示输出 + 接触器两个区块，点击输出仅切换接触器列表。

### 6.14 开关灯操作流程

```text
点击「开灯」：
  1. POST /station/command/CheckOutputHasWaterAlarm  body: {"ids":[通道id]}
     返回 boolean：
       true  → 确认框「发现水浸报警,确定执行开灯操作?」→ 确认后继续
       false → 直接继续
点击「关灯」：直接进入下一步。

  2. 弹出时间选择器：
     标题：开灯「保持开灯状态到何时，之后进入自动运行。」
           关灯「保持关灯状态到何时，之后进入自动运行。」
  3. 用户选好时间 chooseTime（格式 yyyy-M-d H:m:00）后：
     POST /station/command/SendOutputOld
     body: {
       "code": "handControl",
       "list": [通道id]（勾选「控制所有通道」时为全部输出通道 id）,
       "checkUserId": 当前用户id,
       "args": { "isOpen": true/false, "isEnable": true,
                 "isPoint": false, "expire": chooseTime }
     }
  4. 显示加载中；发送接口返回 cmdId。
  5. WebSocket 回执（见 2.6/6.15）：成功 → 弹「执行结果」弹窗并刷新本页；
     超时(7)/失败(8) → toast。
```

### 6.15 底部操作栏

前置判断：无网络 → 提示无网络；召测/时钟类操作要求已配置总配电（`mains[0].id > 0`），否则 toast「请先配置总配电」。

| 按钮   | 确认框          | 指令代码        | 加载文案          | 发送接口                                    |
|------|--------------|-------------|---------------|-----------------------------------------|
| 召测   | 确定发送召测指令吗？   | `forceRead` | 正在执行召测命令...   | `POST /station/command/SendPowerboxOld` |
| 查询时钟 | 确定发送查询时钟指令吗？ | `getclock`  | 正在执行查询时钟命令... | 同上                                      |
| 校准时钟 | 确定发送校准时钟指令吗？ | `setclock`  | 正在执行校准时钟命令... | 同上                                      |
| 导航   | 无            | -           | -             | 打开地图导航弹层（当前定位 → 站点 lat/lng，百度坐标系）       |

指令 JSON 结构：`{ "code": 指令代码, "list": [总配电设备id], "checkUserId": 用户id, "args": {} }`。

WebSocket 回执处理（`type == "cmd"` 且 `commandId == 本页cmdId`）：

- `status == 9` 且 `cmdCode ∈ {handControl, forceRead, getclock, setclock}`：
  - `getclock` → 弹窗内容「设备当前时间：{content.nowTime}」；
  - 其它 → 弹窗内容「执行成功」；
  - 关闭加载、刷新整页数据。
- `status == 7` → toast「指令超时」；`status == 8` → toast「执行失败」。

### 6.16 右上角菜单

| 菜单项      | 行为                                                  |
|----------|-----------------------------------------------------|
| 扫码（图标按钮） | 相机权限 → 扫码（见 6.17）                                   |
| 单灯（图标按钮） | → 单灯详情页，参数：`stationId`、`deviceId=总配电设备id`、`boxName` |
| 配电箱属性    | 属性管理页（定位到总配电 tab）                                   |
| 设备管理     | 站点设备列表页                                             |
| 灯杆管理     | 灯杆列表页                                               |
| 空灯杆      | 空灯杆列表页                                              |
| 站点图片     | 站点图库                                                |
| 编辑位置     | 地图选点，保存返回后自动刷新本页                                    |
| 手动添加设备   | 手动添加弹窗                                              |
| 查找设备     | 按设备号查找所属站点                                          |

（源码中「图库」「配电箱操作记录」「指令发送记录」为隐藏项，可不实现。）

### 6.17 扫码添加流程

1. 申请相机权限，拒绝则弹「开启相机权限」引导弹窗。
2. 扫码结果分两类：
   - 内容包含 `alarm/upload`：取最后一个 `=` 之后的内容为灯杆 key → `POST /station/alarm/QueryUploadAlarm` → 弹「添加灯杆」弹窗。
   - 其它：内容去掉换行、去掉 `ID` 字样 → `POST /station/config/GetLightDeviceInfo`：
     - `isExist == true` → toast「设备已存在」+ 确认框可跳「查找设备」页；
     - `type == 1 / 2` → 弹「添加控制器」弹窗；
     - `type == 3` → 弹「添加单灯」弹窗；
     - 接口报错 → 弹「二维码信息提示」弹窗。
3. 添加成功后的后续操作弹窗（继续扫码 / 完成）。

---

## 7. 单灯详情页（SingleDetailActivity）

### 7.1 入口参数

| 参数          | 必填 | 说明                             |
|-------------|----|--------------------------------|
| `stationId` | 是  | 站点 id                          |
| `boxName`   | 是  | 站点名（作为导航栏标题）                   |
| `parentId`  | 否  | 左树分组 id（作为筛选 `groupId` 传给列表接口） |

### 7.2 页面结构

```text
┌────────────────────────────────────────────┐
│ ← │ 站点名称                        │ ⋮菜单 │
├────────────────────────────────────────────┤
│ 筛选卡片（默认收起，↑↓箭头控制展开）         │
│  名称[输入框≤13字]        分组[下拉选择]     │
│  日表[亮灯计时日表/准时日表]      [查询]     │
├────────────────────────────────────────────┤
│ ☑全选 共N │ [亮][亮警][离线][灭][灭警] │ ⌃ │ ⇄ │
├────────────────────────────────────────────┤
│ (列表模式) 表头: 名称 状态 电压↕ (功率)(亮度) 最近时间↕ │
│ 分页条(仅总数>100): 100条/页▾ 共X页 ◀ [1] ▶ │
├────────────────────────────────────────────┤
│ 单灯网格(默认3列) / 列表                    │
├────────────────────────────────────────────┤
│ (召测)(开灯)(关灯)(调光)(色温)   ← 圆形图标按钮行 │
│ [查询时钟][校准时钟][设置日表][控制模式][清除指令] │
└────────────────────────────────────────────┘
```

- 标题：站点名，>14 字用 14sp，否则 18sp。
- 底部第一行按钮整体受 `dco` 权限控制（无权限隐藏）。
- 底部第二行按钮与筛选卡片同显隐（展开筛选时出现）。

### 7.3 数据加载

主请求（列表）：`POST /station/config/QueryLightByFilter`

```json
{
  "stationId": "站点id",
  "group": 0,            // 单灯分组筛选，默认0
  "code": "",
  "name": "名称关键字",
  "timeId1": 0,          // 亮灯计时日表 id
  "timeId2": 0,          // 准时日表 id
  "online": 0,           // 0全部 1在线 2离线
  "alarm": 0,            // 0全部 1报警 2无报警
  "running": 0,          // 0全部 1亮灯 2关灯
  "index": 1,            // 页码
  "size": 100,           // 每页数量
  "startTime": "2025-06-01",
  "endTime": "当天日期",
  "groupId": 0           // 左树分组id（入口参数 parentId）
}
```

响应：`data.list` → 单灯数组（空结果时后端返回错误信息「未查到相关数据」，此时清空列表）。

状态分类数量：`POST /station/config/QueryLightStateByStationId`，body `{"stationId": id}` → `{total, on, off, onAlarm, offAlarm, offline}`。

后台附加加载（进入页面约 500ms 后启动，任务间隔 200ms，互不阻塞）：

| 任务     | 接口                                                                    | 用途                                   |
|--------|-----------------------------------------------------------------------|--------------------------------------|
| 单灯分组   | `POST /station/config/QueryArea {"stationId"}`                        | 筛选下拉「分组」                             |
| 单灯计时日表 | `POST /station/plan/QueryLight6List`                                  | 筛选下拉「日表」（type=6）                     |
| 设备列表   | `POST /station/config/QueryDevice {"type":2,"groupId":0,"stationId"}` | 群调时的集中器设备                            |
| 单灯准时日表 | `POST /station/plan/QueryLight8List`                                  | 筛选下拉「日表」（type=8）                     |
| 总配电    | `POST /station/config/QueryMain {"groupId":0,"stationId"}`            | 取 `mains[0].deviceId` 作为清除指令队列的设备 id |

日表下拉数据组装：列表首位插入伪选项「全部计时日表」/「全部准时日表」（id=0），其余每项名称后缀 ` -- 计时日表` / ` -- 准时日表`。

### 7.4 单灯原始数据与包装（SingleDataUtil）

原始单灯 `SingleDetailBean.DataBean` 关键字段：

| 字段                                           | 说明                |
|----------------------------------------------|-------------------|
| `id`                                         | 单灯 id             |
| `name`                                       | 单灯名               |
| `code`                                       | 通信 ID             |
| `stationId` / `deviceId`                     | 站点 / 所属设备         |
| `pole` / `poleName`                          | 绑定灯杆 id / 灯杆名     |
| `type`                                       | 类型（`102` = 太阳能单灯） |
| `online` / `running` / `alarm`               | 状态三要素             |
| `fireTime`                                   | 最近数据时间（毫秒）        |
| `timeName` / `areaName`                      | 时间表名 / 分组名        |
| `content.en1~en4`                            | 一~四路通道是否启用        |
| `content.nm1~nm4`                            | 通道名               |
| `content.pole` / `content.area`              | 灯杆 / 分组           |
| `lastData.u`                                 | 单灯电压              |
| `lastData.c1~c4`                             | 各通道电流             |
| `lastData.p1~p4`                             | 各通道功率             |
| `lastData.op1~op4`                           | 各通道亮度百分比          |
| `lastData.cl1~cl4`                           | 各通道漏电             |
| `lastData.oc1~oc4`、`q1~q4`、`ct1~ct4`、`w1~w4` | 各通道其它遥测           |
| `lastData.tc`                                | 色温                |
| `lastData.tv` / `lo`                         | 时间表状态 / 继电器       |

包装规则（每盏灯生成一个显示对象）：

1. 对每个启用通道（`en1~en4`）生成一行通道数据：通道名 `K1:{nm1}`、电流 `c1`、功率 `p1`（取整 + `W`）、亮度 `op1`（取整 + `%`）、漏电 `cl1` 等。
2. 多通道的字段用 `\n` 连接（一行显示一个通道的值，多行排布）。
3. 数值格式化：负数一律显示 `-`；电压保留 1 位小数，整数值去掉小数点。
4. 电压 = `lastData.u`；色温 = `lastData.tc`；最近时间 = `fireTime`。
5. 通道清单 = 启用通道集合 `[{channel:0,name:"一路"}...]`，用于操作弹窗的通道勾选。

显示对象（SinglePackageBean）供列表使用的核心字段：`id`、`name`、`poleName`、`voltage`、`ampere`、`power`、`brightness`、`lastTime`、`online/running/alarm`、`type`、`pole`、`connectId`(通信ID)、`channelList`。

### 7.5 状态筛选栏

横向 5 个「图标 + 数量」项，**单选、可再次点击取消**，顺序与筛选参数映射：

| 位置 | 图标                       | 数量字段       | 含义   | 筛选参数 (online, running, alarm) |
|----|--------------------------|------------|------|-------------------------------|
| 0  | `single_on`              | `on`       | 开灯   | (1, 1, 2)                     |
| 1  | `single_on_alarm`        | `onAlarm`  | 开灯报警 | (1, 1, 1)                     |
| 2  | `single_line_off_tile`   | `offline`  | 离线   | (2, 0, 0)                     |
| 3  | `single_off_alarm`       | `off`      | 关灯   | (1, 2, 2)                     |
| 4  | `single_tile_off_alarm2` | `offAlarm` | 关灯报警 | (1, 2, 1)                     |

交互规则：

- 选中 → 用对应参数重新请求列表；左上角总数显示该状态数量。
- 再次点击取消 → 恢复全部（总数显示 `total`）。
- 选中状态数量为 0 → 不发请求，直接清空列表、总数显示 0。
- 筛选为「离线」时，底部所有指令操作被拦截（见 7.11）。

### 7.6 两种显示模式

右上角模式按钮切换（图标 `iv_list_mode` / `iv_grid_mode`），切换后重置排序状态并整体刷新。

网格模式（默认）：3 列卡片，每卡 = 状态图标（五态）+ 名称 + 功率/亮度（`show_power` 开）或占位；选中换背景色。

列表模式：每行 = 复选框 | 名称 | 状态图标 | 电压 |（功率 | 亮度，受 `show_power` 控制）| 最近时间（`yyyy-MM-dd HH:mm:ss`）。表头存在时功率/亮度列单位去掉（W、%）。

通用设置项：

- `show_pole` 开 → 名称显示为「灯杆名 + 单灯名」。
- 交互：
  - 点卡片整体（网格）/ 复选框（列表）→ 选中/取消选中；
  - 点名称 → 改名弹窗（需 `dco` 权限，静默校验）：`POST /station/config/ChangeLightName {"id", "name"}`，若同时改通信 ID 则 `POST /station/config/ChangeLightNameAndCode {"id","name","code"}`；
  - 长按名称 → 确认「确定要删除当单灯吗?」→ 删除属性接口（`[单灯id]`）；
  - 点状态图标 → 详情弹窗：`type == 102` 弹太阳能单灯信息；`pole != 0 && poleName` 存在弹普通单灯信息；否则 toast「单灯未绑定灯杆」。

### 7.7 全选与选中集合

- 全选 = 当前列表（当前页、筛选/排序后）的全部条目。
- 页面维护一个「已选集合」，所有指令操作针对该集合。
- 列表为空时点全选 → toast「没有单灯数据」。
- 翻页、刷新、清除条件、排序后都会清空选中。

### 7.8 排序（列表模式表头）

- 电压列：点击循环「升序 → 降序 → 默认」；默认恢复按名称排序；图标依次为 `iv_sort_down`、`iv_sort_up`、`iv_sort`。
- 最近时间列：点击循环「降序 → 升序 → 默认」，规则同上。
- 排序作用于本地完整数据集（当前页数据），排序后清空选中。
- 内部还保留名称排序开关：在「按 id 排序」与「按 name 排序」间切换（源码中该按钮隐藏，可不做）。

### 7.9 分页

- 每页数量选项：100 / 200 / 500 / 1000 / 2000 条/页（默认 100）。
- 仅当总数 > 100 时显示分页条。
- 总页数 = ⌈总数 ÷ 每页数量⌉，显示「共 X 页」。
- 页码选择：总页数 > 1 时弹选择器；否则 toast「最多只有一页」。
- 上/下页带边界保护；翻页后清空选中并重新加载。

### 7.10 筛选查询

- 查询入口：筛选卡片「查询」按钮；名称、分组、日表至少填一项，否则 toast「未选择查询条件」。
- 分组下拉：来自 `QueryArea`；选中记录分组 id。
- 日表下拉：合并后的日表列表；`type == 6` 写入 `timeId1`（亮灯计时日表），`type == 8` 写入 `timeId2`（准时日表）。
- 「清除条件」：重置全部筛选/排序/分页状态并重新加载（源码中该按钮隐藏，可用下拉刷新替代）。

### 7.11 底部操作按钮

统一前置校验顺序：

1. 当前状态筛选为「离线」（位置 2）→ toast「设备离线状态,无法发送指令.」。
2. 无网络 → 提示。
3. 未选中任何单灯 → toast「请选择要操作的单灯设备」。

| 按钮   | 弹窗              | 指令代码                          | 发送接口                                                                             |
|------|-----------------|-------------------------------|----------------------------------------------------------------------------------|
| 召测   | 确定召测选中设备？       | `forceRead`                   | `POST /station/command/SendLampOld`                                              |
| 开灯   | 开灯弹窗（勾选通道）      | `handSingle`                  | 同上                                                                               |
| 关灯   | 关灯弹窗（勾选通道）      | `handSingle`                  | 同上                                                                               |
| 调光   | 调光弹窗（勾选通道 + 亮度） | `handSingle`                  | 同上                                                                               |
| 色温   | 色温弹窗            | `handSingle`                  | 同上                                                                               |
| 设置日表 | 日表选择弹窗          | `setDayPlan1` / `setDayPlan2` | 同上                                                                               |
| 控制模式 | 模式选择弹窗          | `setPlanType`                 | 同上                                                                               |
| 查询时钟 | 确定查询时钟？         | `getclock`                    | 同上（需 `dco` 权限）                                                                   |
| 校准时钟 | 确定校准时钟？         | `setclock`                    | 同上（需 `dco` 权限）                                                                   |
| 清除指令 | 确定清除当前所有指令？     | -                             | `POST /station/command/QueenClear {"ids":[总配电deviceId]}`（未配置总配电时 toast「请先配置总配电」） |

指令 JSON 形态：

```json
// 召测 / 时钟
{ "code": "forceRead", "list": [设备id, ...], "checkUserId": 用户id, "args": {} }

// 开关灯 / 调光 / 色温
{ "code": "handSingle", "list": [设备id, ...], "checkUserId": 用户id,
  "args": { "expire": 时长, "bright1": 0, "bright2": 0, "bright3": 0, "bright4": 0,
            "color1": 0, "color2": 0, "color3": 0, "color4": 0 } }

// 设置日表
{ "code": "setDayPlan1", "list": [单灯id, ...], "checkUserId": 用户id, "args": {} }

// 控制模式
{ "code": "setPlanType", "list": [单灯id, ...], "checkUserId": 用户id,
  "args": { "ch": 通道, "type": 0 } }
```

说明：

- 弹窗里的通道勾选项 = 所有已选单灯启用通道的并集（一路~四路，去重升序）。
- 每条指令生成一个 36 位 UUID 作为指令 id；发送接口逐条返回结果（`isSuccess` + `message`=cmdId）。
- 发送后立即弹「指令列表」结果弹窗：每条显示「正在执行...」（成功受理）或失败原因；后续 WebSocket 回执按 cmdId 更新对应条目状态。
- 群调（源码中为隐藏入口）：对象改为站点下的集中器设备列表，指令代码 `handAll`、指令名「群调」。

### 7.12 WebSocket 实时更新

- `type == "data"`：`paramId` 命中本页单灯 → 用新的 `lastData` 重新包装该行并局部刷新（不整页刷新）。
- `type == "state"`：`id` 命中本页单灯且 `isIsParam == true && isIsPole == false` → 更新该行 `online/running/alarm` 并局部刷新，同时触发状态分类数量重新统计。

### 7.13 右上角菜单

扫码添加单灯 / 查找设备 / 手动添加单灯 / 设备管理 / 灯杆图片 / 分组管理 / 灯杆管理 / 灯杆地图 / 空灯杆。

### 7.14 扫码

与配电箱页 6.17 相同；灯杆二维码直接传给「添加单灯」弹窗；添加成功后整页刷新。

---

## 8. 接口汇总表

| 接口路径                                         | 方法   | 请求体要点                                                                   | 返回要点                                 | 使用页面       |
|----------------------------------------------|------|-------------------------------------------------------------------------|--------------------------------------|------------|
| `/station/config/QueryGroup`                 | POST | 无                                                                       | 分组数组                                 | 两个树页面      |
| `/station/config/QueryStation`               | POST | `{type:0}`                                                              | 站点数组                                 | 两个树页面      |
| `/station/config/FilterTree`                 | POST | `{filter}`                                                              | 搜索结果（含 isStation）                    | 搜索弹窗       |
| `/sys/auth/DeleteGroupOld`                   | POST | `{id}`                                                                  | 结果                                   | 删除分组       |
| `/station/config/DeleteStation`              | POST | `{list:[id]}`                                                           | 结果                                   | 删除站点       |
| `/station/config/CopyStation`                | POST | 克隆参数                                                                    | 结果                                   | 克隆站点       |
| `/station/alarm/QueryLightDetail`            | POST | `{groupId:0,stationId:0,name:"",type:23,level:-1,start,end,size,index}` | 灭灯报警列表                               | 单灯 Tab     |
| `/station/analyse/EnergyTotal`               | POST | `{groupId:0,stationId}`                                                 | `today` 当日能耗                         | 配电箱详情      |
| `/station/config/QueryPowerBox/`             | POST | `{groupId:0,stationId}`                                                 | 全设备详情                                | 配电箱详情      |
| `/station/command/CheckOutputHasWaterAlarm`  | POST | `{ids:[通道id]}`                                                          | boolean                              | 开灯前检查      |
| `/station/command/SendOutputOld`             | POST | 见 6.14                                                                  | cmdId                                | 输出通道开关灯    |
| `/station/command/SendPowerboxOld`           | POST | `{code,list:[总配电设备id],checkUserId,args}`                                | cmdId                                | 配电箱底部栏     |
| `/station/config/QueryLightByFilter`         | POST | 见 7.3                                                                   | `list` 单灯数组                          | 单灯详情       |
| `/station/config/QueryLightStateByStationId` | POST | `{stationId}`                                                           | 五态数量                                 | 单灯详情       |
| `/station/config/QueryArea`                  | POST | `{stationId}`                                                           | 单灯分组                                 | 单灯详情筛选     |
| `/station/plan/QueryLight6List`              | POST | -                                                                       | 计时日表                                 | 单灯详情筛选     |
| `/station/plan/QueryLight8List`              | POST | -                                                                       | 准时日表                                 | 单灯详情筛选     |
| `/station/config/QueryDevice`                | POST | `{type:2,groupId:0,stationId}`                                          | 设备列表                                 | 单灯详情群调     |
| `/station/config/QueryMain`                  | POST | `{groupId:0,stationId}`                                                 | 总配电数组                                | 单灯详情（清除指令） |
| `/station/command/SendLampOld`               | POST | 见 7.11                                                                  | 逐条 cmdId                             | 单灯全部指令     |
| `/station/command/QueenClear`                | POST | `{ids:[总配电设备id]}`                                                       | 结果                                   | 清除指令队列     |
| `/station/config/ChangeLightName`            | POST | `{id,name}`                                                             | 结果                                   | 单灯改名       |
| `/station/config/ChangeLightNameAndCode`     | POST | `{id,name,code}`                                                        | 结果                                   | 单灯改名+通信ID  |
| `/station/config/GetLightDeviceInfo`         | POST | 二维码内容                                                                   | `{isExist, type, stationName, code}` | 扫码添加       |
| `/station/alarm/QueryUploadAlarm`            | POST | 灯杆 key                                                                  | 灯杆信息                                 | 扫码添加灯杆     |

---

## 9. 枚举与状态码速查

| 枚举 | 值 |
|---|---|
| 站点类型 stationType | 1 配电箱、2 隧道、3 单灯站点、4 水浸站、5 太阳能 |
| 供电方式 supplyMode | 1 市电、2 专变 |
| 筛选参数 online | 0 全部、1 在线、2 离线 |
| 筛选参数 running | 0 全部、1 亮灯(运行)、2 关灯(停止) |
| 筛选参数 alarm | 0 全部、1 报警、2 无报警 |
| 单灯类型 type | 102 太阳能单灯 |
| 转换开关状态 lastData.v | 0 停止、1 手动、2 遥控、3 时控 |
| 接触器状态 lastData.sv | 1 闭合、0 断开、-1 未测量、-2 未配置 |
| 支路电流/漏电特殊值 | -1 未测量、-2 未配置；漏电 0 = 无漏电 |
| 支路时间表状态 lastData.tv | -1 未设置、-2 已删除、0 灭灯时段、1 亮灯时段、2 未启用 |
| 指令状态 status | 2 已发送、4 执行中、5 已重发、7 超时、8 失败、9 成功 |
| 指令代码 | `forceRead` 召测、`getclock` 查询时钟、`setclock` 校准时钟、`handControl` 输出通道开关灯、`handSingle` 单灯控制、`handAll` 群控、`setDayPlan1/2` 设置日表、`setPlanType` 控制模式 |
| 日表类型 | 6 计时日表、8 准时日表 |

---

