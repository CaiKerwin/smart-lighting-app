# 地图模块（MapFragment）开发指导文档

## 1. 页面概述与组成

地图页是一个全屏 Fragment（iOS 对应一个地图页面），使用百度地图（iOS 侧可按 uiapp 选择对应地图 SDK，坐标体系见 §16 说明）。

页面组成：

| 区域 | 说明 |
|------|------|
| 顶部标题栏 | 「GIS 地图」 |
| 地图主体 | 普通地图 / 卫星地图切换 |
| 顶部横向 CheckBox 列表（`rvMapCb`） | 各类标注物的显示开关（配电箱 / 单灯 / 专变 / 水浸 / 线缆） |
| 左侧竖排状态栏（`rvStateType`） | 单灯状态分类筛选（正常亮灯 / 开灯报警 / 正常灭灯 / 关灯报警 / 离线），勾选「单灯」图层时显示 |
| 底部/侧边按钮 | 定位、普通/卫星切换、刷新、搜索 |

---

## 2. 术语与常量对照

### 2.1 标注物「地图点击类型」（`DeviceTypeUtil`，用于 marker 的 extraInfo 与点击分发）

| 常量 | 值 | 含义 |
|------|----|------|
| `MAP_BOX` | 1 | 配电箱标注物 |
| `MAP_LIGHT` | 2 | 灯杆标注物 |
| `MAP_LUX` | 3 | 光控标注物 |
| `MAP_CABLE` | 4 | 线缆 |
| `MAP_GROUP` | 5 | 分组 |
| `MAP_SPECIALIZED` | 6 | 专变标注物 |
| `MAP_WATER` | 7 | 水浸标注物 |

### 2.2 业务类型（`Constant`）

| 常量 | 值 | 含义 |
|------|----|------|
| `TYPE_BOX` | 0 | 配电箱 |
| `TYPE_SPECIALIZED` | 2 | 专变（同时也是 supplyMode=2 的判定值） |
| `TYPE_WATER` | 3 | 水浸（地图选点类型） |
| `TYPE_SOLAR` | 5 | 太阳能灯杆 |
| `TYPE_POLE` | 14 | 灯杆 |
| `TYPE_LIGHT` | 199 | 单灯 |
| `TYPE_LUX` | 299 | 光控 |

### 2.3 `MapMarkBean.supplyMode` 语义（注释）

| supplyMode | 含义 |
|-----------|------|
| 1 | 配电箱 |
| 2 | 专变 |
| 4 | 水浸 |

### 2.4 缩放级别阈值（关键）

| 常量 | 值 | 作用 |
|------|----|------|
| `defaultZoom` | 18.0f | 默认缩放 |
| `showMarkText` | 18.0f | 显示详细标注物（含名字）的阈值 |
| `showPoleZoom` | 18.2f | 配电箱从海量点切换为详细标注物的阈值 |

### 2.5 状态字段组合（`online` / `running` / `alarm`）

所有标注物（配电箱、灯杆、专变、水浸）的图标都基于这三个布尔字段 +（灯杆额外 `count`）判断：

| 状态 | 条件 |
|------|------|
| 在线-运行-无报警（开灯） | `online && running && !alarm` |
| 在线-运行-报警（开灯报警） | `online && running && alarm` |
| 在线-未运行-无报警（关灯） | `online && !running && !alarm` |
| 在线-未运行-报警（关灯报警） | `online && !running && alarm` |
| 离线 | `!online` |

---

## 3. 通用请求 / 响应约定

与「扫码添加设备」文档一致（必须复刻）：

- **请求头**：`auth`（token）、`Custid`、`Apptype`、`Lang`。
- **请求体封装**：POST/PUT 统一 `{"content": Base64(业务JSON)}`。
- **响应封装**：`{code, data, abab}`，`code==0` 成功，`abab!="false"` 时 `data` 为 `Base64(AES)` 密文需解密；`data` 为 null/`[]`/`{}` 按「暂无数据」处理。
- 业务失败走 `errorLiveData(ErrorBean{errorCode,errorMessage,errorUrl})`，页面按 `errorUrl` 区分接口。

---

## 4. 数据接口清单

| # | 路径 | 方法 | 用途                                                   | 请求体（业务 JSON） |
|---|------|------|------------------------------------------------------|---------------------|
| 1 | `/station/gis/QuerySimple` | POST | 地图标注物（配电箱/灯杆/光控/专变）                                  | `{"groupId":0,"stationId":0}` |
| 2 | `/station/water/QueryGisItems` | POST | 水浸地图数据                                               | `{"groupId":0,"stationId":0}` |
| 3 | `/station/gis/QueryLines` | POST | 地图线缆                                                 | `{"groupId":0,"stationId":0}` |
| 4 | `/station/gis/MakerInfo/` | POST | 单个标注物详情                                              | `{"id":<id>,"type":<type>}` |
| 5 | `/station/gis/PoleInfo` | POST | 灯杆详细信息（含杆上单灯列表）                                      | `{"id":<poleId>}` |
| 6 | `/station/config/QueryPoleByLight` | POST | 按单灯通信 ID 查所属灯杆                                       | `{"code":"<8位ID>"}` |
| 7 | `/station/config/QueryLightBrightColorConfig` | POST | 单灯色温范围                                               | `{"id":<lightId>}` |
| 8 | `/sys/setting/QueryLightColumns` | POST | 单灯显示列配置（判断是否显示漏电）根据是否有cl权限判断单灯详情和灯杆详细界面是否显示漏电电流这一行数据 | `{}` |
| 9 | `/station/gis/SetPos` | POST | 设置标注物（配电箱/灯杆）经纬度                                     | `{"id":"<id>","type":<type>,"lat":x,"lng":y}` |
| 10 | `/station/water/SetPos` | POST | 设置水浸经纬度                                              | `{"id":"<id>","lat":x,"lng":y}` |

> `MakerInfo` 详情接口按 `type` 返回不同结构（见 §17）：`TYPE_BOX`→MapMarkBean[]、`TYPE_LIGHT`/`TYPE_SOLAR`→单灯详情、`TYPE_LUX`→光控详情。

---

## 5. 数据加载与分类分发

### 5.1 初始化请求（`initData`）

进入页面即请求三个接口：

```
getMapDataList()  → QuerySimple  → MapMarkBean[] → poleListViewModel
getWaterList()    → QueryGisItems → MapWaterBean[] → waterListViewModel
getLine()         → QueryLines   → MapLineBean[]  → lineViewModel
```

### 5.2 标注物分类（`poleListViewModel` 回调，核心逻辑）

`QuerySimple` 返回的 `MapMarkBean[]` 是**混合列表**，按字段分类：

```kotlin
dataList.forEach {
    if (it.type == TYPE_POLE /*14*/) {
        mPoleList.add(it)                     // 灯杆
    } else if (it.type == 0 && it.stationType == 1) {
        if (it.supplyMode == TYPE_SPECIALIZED /*2*/) {
            mSpecializedList.add(it)          // 专变
        } else if (it.supplyMode == 1) {
            mBoxList.add(it)                  // 配电箱
        }
    }
    // 光控 type == TYPE_LUX(299) 的分支被注释掉，光控暂不参与此接口分类
}
```

分类完成后：
- 若「配电箱」图层已勾选 → `showBoxOnMap()`
- 若「专变」图层已勾选 → `setSpecializedData(mSpecializedList)`
- 若「单灯」图层已勾选 → `showPoleOnMap()`
- 更新状态栏 `initStateBarData()`、更新 CheckBox 列表 `initCbList()`

---

## 6. 地图初始化与定位

### 6.1 初始化

```kotlin
mBaiduMap = mapView.map
mBaiduMap.showMapIndoorPoi(false)          // 隐藏室内 POI
mBaiduMap.uiSettings.isOverlookingGesturesEnabled = false
mBaiduMap.mapType = MAP_TYPE_NORMAL
mBaiduMap.isMyLocationEnabled = true
```

### 6.2 权限

请求 `ACCESS_FINE_LOCATION`；允许 → `updateMapData()`；拒绝 → 弹权限提示。

### 6.3 中心点选择（`loadLocation`，优先级）

1. 本地缓存 `sun_lat` / `sun_lng`（项目配置的中心坐标）非空 → 地图以该坐标为中心，zoom=18。
2. 否则使用当前定位坐标。
3. 「定位」按钮：以当前定位为中心，`isSetZoom=false`（不重置 zoom）。

### 6.4 手势结束回调（`onMapStatusChangeFinish`）

```kotlin
if (zoom > 4.2) {
    if (mPoleList.isNotEmpty()) showPoleOnMap()   // 动态筛选可视范围灯杆
    if (mBoxList.isNotEmpty()) showBoxOnMap()     // 显示配电箱
} else {
    clearBoxText()                                 // 缩放太小时清除配电箱名字
}
```

> 即：手势停止后，按当前可视范围（bound）+ 当前缩放重新渲染灯杆/配电箱标注物，实现「边拖边筛」。

---

## 7. 标注物图层开关（CheckBox 列表）

### 7.1 列表项（`initCbList`，横向 RecyclerView）

| position | 显示名 | 数量来源 |
|----------|--------|---------|
| 0 | 配电箱 | `mBoxList.size` |
| 1 | 单灯（灯杆） | `mPoleList.size` |
| 2 | 专变 | `mSpecializedList.size` |
| 3 | 水浸 | `mWaterList.size` |
| 4 | 线缆 | `mLineList.size` |

`cbStateList` 数组记录 5 个开关状态（初始全 false）。

### 7.2 点击处理（`handleCbEvent`）

| position | 勾选 | 取消 |
|----------|------|------|
| 0 配电箱 | `showBoxOnMap()` | `clearBoxMark()` + `clearMultiBoxMark()` |
| 1 单灯 | `showPoleOnMap()` + 显示状态栏 | `clearMultiPoleMark()` + `clearBigPole()` + 隐藏状态栏 |
| 2 专变 | `setSpecializedData(mSpecializedList)` | `clearSpecializedMark()` |
| 3 水浸 | `setWaterData(mWaterList)` | `clearWaterMark()` |
| 4 线缆 | `setLineData()` | `clearLine()` |

> 代码中存在 `case 5`（光控）分支，但 CheckBox 列表只初始化 5 项（0–4），光控分支实际不可达（预留）。

---

## 8. 灯杆状态筛选栏

仅在「单灯」图层勾选后显示（`rvStateType` 竖排 RecyclerView）。

### 8.1 状态分类统计（`initStateBarData`）

基于 `mPoleList`（全部灯杆）统计：

| 状态 | 统计条件 | 图标（示意） |
|------|---------|-------------|
| 正常亮灯 | `online && running && !alarm` | single_on |
| 开灯报警 | `online && running && alarm` | single_on_alarm |
| 正常灭灯 | `online && !running && !alarm` | single_off_alarm |
| 关灯报警 | `online && !running && alarm` | single_tile_off_alarm2 |
| 离线 | `!online` | single_line_off_tile |

### 8.2 筛选（`filterPole` + `mStateBarCheckedMap`）

`mStateBarCheckedMap` 记录当前选中的状态位置（单选，勾选一个会取消其它）。`filterPole` 时若存在选中状态，则按状态过滤灯杆：

| position | 过滤条件 |
|----------|---------|
| 0 | `online && running && !alarm` |
| 1 | `online && alarm` |
| 2 | `online && !running && !alarm` |
| 3 | `online && !running && alarm` |
| 4 | `!online` |
| 无选中（全 false） | 不过滤 |

---

## 9. 各类标注物显示逻辑（重点）

所有标注物坐标都先经过 `filterPole`（按可视范围 bound 过滤），`lat ∈ [latMin, latMax]`、`lng ∈ [lngMin - offset, lngMax + offset]`，offset：zoom>=17 为 0.001，否则 0.0001。

### 9.1 配电箱（`showBoxOnMap` / `setBoxData` / `addBoxMultiMark`）

- 缩放 `zoom >= 18.2`：显示详细 marker（`setBoxData`）；否则显示海量点（`addBoxMultiMark`）。
- 图标（5 态）：

| 状态 | 图标 |
|------|------|
| 在线-运行-无报警 | `box_on_new` |
| 在线-运行-报警 | `box_on_alarm_new` |
| 在线-未运行-无报警 | `box_off_new` |
| 在线-未运行-报警 | `box_off_alarm_new` |
| 离线 | `box_offline_new` |

- extraInfo（点击用）：`STATION_ID`(id 字符串)、`BOX_NAME`(name)、`LATITUDE`、`LONGITUDE`、`TYPE`=1。
- 名称：`zoom >= 18.0` 时在图标下方加文字（`addText`，文字偏移 `lat - 0.000015`）。

### 9.2 灯杆（`showPoleOnMap` / `addPoleData` / `addPoleMultiMark`）

- 缩放 `zoom >= 18.0`：显示详细 marker（`addPoleData`）；否则海量点（`addPoleMultiMark`）。
- 图标：由「状态 + 灯数量 count」共同决定（`PoleImgUtil`），5 态 × 4 档：

| 状态 | count=0 | 1 | 2 | 3 | >=4 |
|------|---------|---|---|---|-----|
| 开灯 | `pole_none` | `pole_on_1` | `pole_on_2` | `pole_on_3` | `pole_on_4` |
| 开灯报警 | `pole_none` | `pole_on_alarm_1` | `pole_on_alarm_2` | `pole_on_alarm_3` | `pole_on_alarm_4` |
| 关灯 | `pole_none` | `pole_off_1` | `pole_off_2` | `pole_off_3` | `pole_off_4` |
| 关灯报警 | `pole_none` | `pole_off_alarm_1` | `pole_off_alarm_2` | `pole_off_alarm_3` | `pole_off_alarm_4` |
| 离线 | `pole_none` | `pole_offline_1` | `pole_offline_2` | `pole_offline_3` | `pole_offline_4` |

> `count` 表示该灯杆下的单灯数量，图标上的「灯数」用 1–4 档表示（>=4 统一用 4 档图标）。

- extraInfo：`POLE_ID`(id)、`TYPE`=2。
- 名称：同配电箱，`zoom >= 18.0` 时加文字。
- 海量点 title：JSON `{"id":<id>,"type":"1"}`（type "1" 表示灯杆海量点）。

### 9.3 专变（`setSpecializedData` / `setSpecializedMarker`）

- 图标（5 态）：

| 状态 | 图标 |
|------|------|
| 在线-运行-无报警 | `specialized_on` |
| 在线-运行-报警 | `specialized_on_alarm` |
| 在线-未运行-无报警 | `specialized_off` |
| 在线-未运行-报警 | `specialized_off_alarm` |
| 离线 | `specialized_offline` |

- extraInfo：`STATION_ID`(id 字符串)、`BOX_NAME`(name)、`LATITUDE`、`LONGITUDE`、`TYPE`=6。
- 名称：`addText`，偏移同上。

### 9.4 水浸（`setWaterData` / `setWaterMarker`）

- 图标（3 态）：

| 状态 | 图标 |
|------|------|
| 在线-报警 | `water_map_alarm` |
| 在线-无报警 | `water_map_normal` |
| 离线 | `water_map_offline` |

- extraInfo：`ID`(String)、`STATION_ID`(stationId 字符串)、`NAME`、`LATITUDE`、`LONGITUDE`、`WATER_BEAN`(完整 MapWaterBean JSON 字符串)、`TYPE`=7。
- 名称：`addText`。

### 9.5 光控（`setLuxData` / `setLuxMarker`）

- 图标：固定 `iv_lux_map`。
- extraInfo：`ID`(Int)、`NAME`、`TYPE`=3。
- 注意：光控数据来源于 `getMapDataList` 但分类分支被注释，当前不实际显示（预留逻辑）。

### 9.6 线缆（`setLineData`）

- 只画 `points.size > 1` 的线缆，`PolylineOptions`：颜色 `colorBlue`、宽度 0。
- extraInfo：`ID`、`NAME`。
- 线缆点击只打印日志，**无弹窗**。

### 9.7 图标缓存与缩放

`getMarkerIcon(resId)`：所有图标统一缩放到原尺寸 0.7 倍，并缓存（避免重复解码）。

---

## 10. 海量点（MultiPoint）机制

当缩放不足时，配电箱 / 灯杆改用「海量点」渲染（一个覆盖物承载大量点，性能优化）。

- 灯杆海量点图标：`markPoleIvRes(zoom)`，按 zoom 返回 `iv_point_1` / `iv_point_2`（<15 用 iv_point_2，15–17 用 iv_point_1，>=17 用 iv_point_2，实际多数档位是 iv_point_2）。
- 配电箱海量点图标：`markBoxIvRes(zoom)`，`iv_box_point_1` / `iv_box_point_2`（13–15 用 iv_box_point_2，其余 iv_box_point_1）。
- 海量点点击通过 `title` 字段传递信息（见 §11）。

---

## 11. 标注物点击事件与弹窗

### 11.1 普通 marker 点击（`onMarkerClick` → `handleMarkerClickEvent`）

从 marker 的 `extraInfo` 取出 `TYPE`，分发：

| TYPE | 弹窗 | 说明 |
|------|------|------|
| 1（配电箱）/ 6（专变） | `BoxOperationDialog` | 配电箱/专变操作弹窗 |
| 2（灯杆） | `PoleInfoDialog` | 灯杆详情弹窗 |
| 7（水浸） | `WaterMapDialog` | 水浸详情弹窗 |
| 3（光控） | `MapLuxInfoDialog` | 光控详情弹窗 |

#### 11.1.1 配电箱 / 专变点击 → `BoxOperationDialog`

- 标题 = `BOX_NAME`（名称）。
- 三个按钮（`OnBoxOperationListener`）：
  - **查看详情** `checkDetail()`：路由跳转 `powerBox`（携带 `STATION_ID`、`BOX_NAME`）→ 打开配电箱详情页。
  - **导航** `navigation()`：`MapBottomSheetDialog`（见 §15），起点=当前定位，终点=标注物坐标。
  - **编辑位置** `editLocation()`：`ChoosePointMapActivity`（type=`TYPE_BOX`，携带 ID/lat/lng/name，见 §16）。

#### 11.1.2 灯杆点击 → `PoleInfoDialog`

- 取出 `POLE_ID`，`PoleInfoDialog.newInstance(poleId)`（见 §12）。

#### 11.1.3 水浸点击 → `WaterMapDialog`

- 传入完整 `MapWaterBean` JSON 字符串，弹窗展示：
  - 名称 `name`
  - 基础水位 `base`（`base==0 → "0 cm"`，否则 `base/10 cm`）
  - 实际水位 `result`（`result==0 → "0 cm"`，否则 `result/10 cm`）
  - 水位 `level`（`level==0 → "0 cm"`，否则 `level/10 cm`；`isAlarm` 红色否则强调色）
  - 在线/离线（`isOnline` → "在线" / "离线"，配颜色）
  - 是否报警（`isAlarm` → "是"/"否"）
  - 水位计 `cupView`：maxScale=30，`setWaterLevel(level/300, isAlarm)`
  - 速度 `speed`
- 同样三个按钮：查看详情（路由 `waterBox`，携带 `STATION_ID`）、导航、编辑位置（type=`TYPE_WATER`）。

#### 11.1.4 光控点击 → `MapLuxInfoDialog`

- 显示名称 `NAME`；`getMarkDetail(id, TYPE_LUX)` 取详情。
- 展示光照值 `lastData.ls`（列表/照度值）、中值 `lastData.l`。
- 「操作」按钮当前为空实现；「导航」按钮 → `MapBottomSheetDialog`。

### 11.2 海量点点击（`onMultiPointClick`）

从 `title` JSON 解析：

| title.type | 行为 |
|-----------|------|
| `"1"`（灯杆） | `PoleInfoDialog(id)` |
| `"2"`（配电箱） | 取 name/lat/lng → `showBoxOprDialog(name, id, lat, lng)`（即 `BoxOperationDialog`） |

---

## 12. 灯杆详情弹窗（PoleInfoDialog + 单灯 Tab）

### 12.1 流程

1. 进入即调 `getPoleInfo(poleId)` → `POST /station/gis/PoleInfo {id}` → `PoleInfoBean`。
2. 返回后：
   - 顶部显示灯杆名 `poleBean.name`、灯数 `poleBean.lights.size`。
   - 用 `ViewPager2` + `TabLayout` 展示该灯杆下每个单灯（每灯一个 Tab，Tab 标题 = 该灯 `name`）。

### 12.2 `PoleInfoBean`（灯杆详情响应）

```json
{ "name": "", "pathName": "", "code": "", "lat": 0.0, "lng": 0.0,
  "lights": [ { "id": 1, "name": "灯1", ...LightBean } ] }
```

### 12.3 单灯 Tab（`MapLightFragment`）

每个 Tab 是一个单灯详情页，`newInstance(lightId, poleName, lat, lng)`：

- 调 `getMarkDetail(lightId, TYPE_LIGHT)` → 单灯详情。
- 显示字段（`SinglePackageBean`）：
  - 名称 `name`、通道 `channel`、在线状态 `isOnline`（在线/离线）
  - 电压 `voltage`、电流 `ampere`、功率 `power`、亮度 `brightness`、温度 `tempera`
  - 能耗 `energy`、继电器 `relay`、色温 `colorTempera`、漏电 `leakageAmpere`
  - 通信 ID `connectId`（"null" 显示空）、最后通信时间 `lastTime`
- 按钮：
  - **操作单灯**：路由 `lightList`（携带 `STATION_ID`、`BOX_NAME`、`LIGHT_NAME`）→ 打开单灯列表页操作。
  - **编辑位置** / 灯杆图标：`ChoosePointMapActivity`（type=`TYPE_POLE`，ID=灯杆 id）。
  - **导航**：`MapBottomSheetDialog`。
  - **图片**：路由 `picActivity`（携带 `STATION_ID`=灯杆 id、`PAGE_TYPE`=POLE_PIC）。
- 特殊：`getValue` 对 `-1`/`-2` 开头或空值显示 `-`；漏电列显示受 `lampColumns()`（`QueryLightColumns`）权限控制（cols 含 "cl" 才显示，否则显示 `-`）。

---

## 13. 单灯详情弹窗（MapLightInfoDialog / SolarLampInfoDialog）

这两个弹窗由「单灯详情列表页」点击单灯时弹出（非地图 marker 直接点击，但属于地图链路下游）：

### 13.1 `MapLightInfoDialog`（普通单灯，type 非 102）

- 参数：`lightId, poleId, poleName, lightType=199, showPole`。
- 数据：`getMarkDetail(lightId, TYPE_LIGHT)` + `getPoleInfo(poleId)` + `lampColumns()` + `getLampColorRange(lightId)`。
- 显示：与 `MapLightFragment` 相同字段集，另加色温换算（`percentToValue`）。
- 按钮：编辑位置、导航、图片。

### 13.2 `SolarLampInfoDialog`（太阳能单灯，type==102）

- 参数同 13.1，但 `getMarkDetail(lightId, lightType, isSolar=true)` 返回 `SolarLampBean`。
- 展示结构化字段（市电/负载/太阳能/蓄电池四大块），含电压、电流、功率、Pwm、累计能耗、电量、是否重复、最后通信时间等。

> 分发入口在单灯详情页 `checkDetail`：`bean.type == 102 → SolarLampInfoDialog`，否则 `MapLightInfoDialog`；且要求 `bean.pole != 0 && bean.poleName != null` 才弹，否则提示「单灯未绑定灯杆」。

---

## 14. 搜索对话框（SearchTabMapDialog）

点击地图「搜索」按钮弹出。

### 14.1 三个 Tab（横向 CheckBox）

| position | 名称 | 搜索方式 |
|----------|------|---------|
| 0 | 道路/地址 | POI 联想搜索（`SuggestionSearch`，city 固定「深圳」） |
| 1 | 配电箱 | 本地过滤 `mBoxList`（按名称包含匹配） |
| 2 | 单灯 | 输入 8 位通信 ID → `getLampPole(code)` 查所属灯杆 |

### 14.2 交互

- **道路**：输入关键词 → POI 联想 → 选中 → `sendLatLng(pt, 18f, 1)` → 回到地图定位。
- **配电箱**：列表显示配电箱（来自 `getMapDataList` 过滤 `type == TYPE_BOX`）→ 选中 → `sendLatLng(latLng, 21f, 2)`。
- **单灯**：输入框实时监听，长度==8 → `getLampPole(code)`，返回后显示灯杆名；点「在地图上查看」→ `sendLatLng(latLng, 21f, 3)`；若输入长度 !=8 提示「请输入8位」，若未查到灯杆提示「未绑定灯杆」。

### 14.3 回传（`sendLatLng`）

```kotlin
sendMsg(VIEW_IN_MAP, ShowMarkOnMapBean(lat, lng, zoom, type))  // type: 1道路 2配电箱 3灯杆
```

`MapFragment.busMsg` 接收 `VIEW_IN_MAP`：
- 设置地图中心点 + zoom。
- `type==2` → `showBoxOnMap()`；`type==3` → `showPoleOnMap()`。

---

## 15. 导航（MapBottomSheetDialog）

底部弹窗，三选一：百度 / 高德 / 腾讯地图导航。

- 输入 `MapPointBean`：`coordinateType`、`startLatitude/startLongitude`（当前定位）、`endLatitude/endLongitude`（目标）、`destinationName`。
- 起点固定显示「我的位置」。
- 若 `coordinateType == GPS` → 先 `gpsToBaiduCoordinate` 转百度坐标；否则直接用。
- 高德/腾讯：还需 `bdToGaoDeCoordinate` 把百度坐标转 GCJ-02。
- 判断对应地图 App 是否安装，未安装提示「请安装百度/高德/腾讯地图」。

---

## 16. 编辑位置（ChoosePointMapActivity / SetPos）

各类标注物弹窗的「编辑位置」都跳转 `ChoosePointMapActivity`：

| 来源 | `TYPE` | 提交接口 | 请求体 |
|------|--------|---------|--------|
| 配电箱/专变 | `TYPE_BOX`(0) | `POST /station/gis/SetPos` | `{"id":"<stationId>","type":0,"lat":x,"lng":y}` |
| 灯杆 | `TYPE_POLE`(14) | `POST /station/gis/SetPos` | `{"id":"<poleId>","type":14,"lat":x,"lng":y}` |
| 水浸 | `TYPE_WATER`(3) | `POST /station/water/SetPos` | `{"id":"<id>","lat":x,"lng":y}` |

`ChoosePointMapActivity` 提交成功后 `setResult(RESULT_OK)`，回到地图后刷新。

> `SetPos` 的 `type` 字段取值：配电箱/灯杆用传入的 `mType`（`TYPE_BOX`=0 或 `TYPE_POLE`=14），水浸走独立接口（不带 type）。

---

## 17. 数据结构 JSON 明细

> 字段名即服务端反序列化 key，iOS 端务必一致。

### 17.1 `MapMarkBean`（QuerySimple 响应，混合标注物）

```json
{
  "id": 0, "sid": 0, "type": 0, "code": "", "name": "",
  "lat": 0.0, "lng": 0.0,
  "online": false, "alarm": false, "running": false,
  "count": 0,          // 灯杆下单灯数量（灯杆图标分档用）
  "supplyMode": 0,     // 1配电箱 2专变 4水浸
  "stationType": 0     // ==1 且 type==0 才进入配电箱/专变分类
}
```

### 17.2 `MapWaterBean`（QueryGisItems 响应）

```json
{
  "id": "", "name": "", "stationId": 0, "lat": 0.0, "lng": 0.0,
  "level": 0, "base": 0, "speed": 0,
  "online": false, "alarm": false, "result": 0, "fireTime": ""
}
```

### 17.3 `MapLineBean`（QueryLines 响应）

```json
{ "id": 0, "stationId": 0, "name": "", "alarm": false, "running": false,
  "points": [ { "lat": 0.0, "lng": 0.0 } ] }
```

### 17.4 `PoleInfoBean`（PoleInfo 响应）

```json
{ "name": "", "pathName": "", "code": "", "lat": 0.0, "lng": 0.0,
  "lights": [ LightBean ] }
```

其中 `LightBean`（单灯）关键字段：`id`、`name`、`stationId`、`deviceId`、`type`、`lat`、`lng`、`online`、`running`、`alarm`、`content`（含 `pole` 灯杆 id、`timeId`、`area` 等）、`lastData`（含 `u` 电压、`c1` 电流、`p1` 功率等大量遥测值）。

### 17.5 光控详情 `LuxDetailBean`（MakerInfo type=3 响应）

```json
{ "id":0, "stationId":0, "type":0, "name":"", "lat":0.0, "lng":0.0,
  "content": { "dx":0, "rule":0, "groups":[] },
  "lastData": { "time":0, "t":0.0, "h":0.0, "l":0, "ts":[], "hs":[], "ls":[] } }
```

> 地图光控弹窗只用到 `lastData.ls`（照度列表）与 `lastData.l`（中值）。

### 17.6 单灯详情 `LightMapDetailBean.DataBean`（MakerInfo type=199 响应）

```json
{ "code":0, "data": { "pathName":"", "code":"",
  "light": { "stationId":0,"deviceId":0,"type":0,"lat":0.0,"lng":0.0,"name":"",
             "content": {...}, "lastData": {...}, "extraData": {...},
             "fireTime":0,"id":0,"online":false,"running":false,"alarm":false } } }
```

### 17.7 太阳能单灯 `SolarLampBean`（MakerInfo type=199 isSolar 响应）

```json
{ "name":"", "code":"", "light": { "isOnline":false, "fireTime":0, "isAllowSameDevice":false,
  "lastData": { "u":0,"c1":0,"p1":0,"freq":0,"acls":0,"acs":0,"acbatpwm":0,"acledpwm":0,
                "acquantity":0,"loadv":0,"loadi":0,"loadp":0,"loads":0,"batledpwm":0,"loadquantity":0,
                "sols":0,"solv":0,"soli":0,"solp":0,"solbatpwm":0,"solquantity":0,
                "bats":0,"batv":0,"batc":0,"batp1":0,"batlv":0,"batquantity":0 } } }
```

### 17.8 其它

- `ShowMarkOnMapBean`（搜索回传）：`{lat, lng, zoom, type}`。
- `MapPointBean`（导航/坐标）：`coordinateType, startLatitude, startLongitude, endLatitude, endLongitude, destinationName`。

---

## 18. iOS 实现对照清单

1. **地图容器**：普通/卫星切换、室内 POI 关闭、定位蓝点、手势监听（停止后按可视范围重绘）。
2. **三接口初始化**：`QuerySimple` + `QueryGisItems` + `QueryLines`，分类（§5.2）。
3. **图层开关**：5 类 CheckBox（配电箱/单灯/专变/水浸/线缆），数量徽标。
4. **单灯状态栏**：5 态统计 + 单选筛选。
5. **标注物渲染**：
   - 图标按 `online/running/alarm`（+灯杆 `count`）5 态/3 态规则（§9）。
   - 缩放阈值切换详细点/海量点（配电箱 18.2，灯杆 18.0）。
   - 名称文字在 zoom>=18 且偏移 `lat-0.000015`。
   - 可视范围过滤（bound + offset）。
6. **点击分发**：marker 的 `TYPE` → BoxOperationDialog / PoleInfoDialog / WaterMapDialog / MapLuxInfoDialog；海量点按 title JSON 分发。
7. **灯杆详情**：`PoleInfo` → 名称/灯数 + ViewPager（每灯 Tab）→ 单灯详情字段。
8. **单灯详情**：`MakerInfo`(type=199) → 字段集 + 色温换算 + 漏电列权限；太阳能单灯走 isSolar。
9. **搜索**：3 Tab（道路 POI / 配电箱本地过滤 / 单灯 8 位查灯杆）→ 回传坐标定位。
10. **导航**：百度/高德/腾讯三选一 + 坐标转换。
11. **编辑位置**：`SetPos` / 水浸 `SetPos` 接口。
12. **刷新**：清缓存 → 重拉三接口 → 重绘。

### 文案对照（关键）

| 场景 | 文案 |
|------|------|
| 请开启定位权限 | （权限提示） |
| GPS 未开 | 请打开GPS定位,以提高定位的准确性. |
| 无光控数据 | 没有光控数据 |
| 未绑定灯杆 | 单灯未绑定灯杆 |
| 未发现灯杆 | 没有发现灯杆 |
| 搜索-请输入8位 | 请输入8位 |
| 搜索-未绑定灯杆 | 未绑定灯杆 |
| 导航-未安装 | 请安装百度地图 / 请安装高德地图 / 请安装腾讯地图 |
| 定位修改成功 | 定位修改成功 |
| 在线/离线 | 在线 / 离线 |
| 报警-是/否 | 是 / 否 |

---

## 19. 注意事项与易踩坑点

1. **分类双重条件**：配电箱/专变必须 `type==0 && stationType==1`，再按 `supplyMode` 区分（1=配电箱，2=专变）。灯杆是 `type==14`，水浸独立接口，光控分类被注释。
2. **`MapMarkBean.id` 在配电箱/专变里实际是站点 id**：配电箱/专变 marker 的 `STATION_ID` 用的是 `bean.id.toString()`，灯杆用 `POLE_ID = bean.id`。
3. **海量点 title 承载数据**：灯杆海量点 title 只存 `{id,type:"1"}`；配电箱海量点 title 存 `{id,type:"2",name,lat,lng}`（点击时无需再查接口）。
4. **水浸 id 是字符串**（`MapWaterBean.id` 为 String），配电箱/灯杆 id 是 int，需注意类型。
5. **缩放阈值差异**：配电箱切详细点阈值 18.2，灯杆 18.0；文字显示阈值 18.0。低于 4.2 清除配电箱文字。
6. **图标 0.7 缩放**：所有 marker 图标统一 0.7 倍，并缓存。
7. **`SetPos` type 语义**：配电箱/灯杆都走 `/station/gis/SetPos`，`type` 传 `TYPE_BOX(0)` 或 `TYPE_POLE(14)`；水浸走 `/station/water/SetPos`（无 type）。
8. **坐标系**：地图为百度坐标（bd09ll），导航高德/腾讯需转 GCJ-02，`coordinateType==GPS` 的起点需 gpsToBaidu。
9. **漏电列权限**：`QueryLightColumns` 返回 cols 不含 `"cl"` 时不显示漏电（显示 `-`）。
10. **单灯详情弹窗前置条件**：需 `pole != 0 && poleName != null`，否则提示「单灯未绑定灯杆」。
11. **`getValue` 空值/-1/-2**：单灯详情字段空或 `-1`/`-2` 开头显示 `-`。
12. **POI 搜索城市固定「深圳」**：iOS 端如需通用，应改为可配置或定位城市。

---

