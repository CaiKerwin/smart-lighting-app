# 扫码添加 / 手动添加设备 —— 开发文档

## 1. 术语与核心常量对照

### 1.1 设备大类 `type`（`QrInfoBean.DataBean.type`）

| type | 名称 | 添加对话框 | 说明 |
|------|------|-----------|------|
| 1 | 采集控制器 | `QrAddControllerDialogFrag` | 需选择「监测电压 / 总配电」开关（`simple`）；非顶级需输入 11/20 位通信 ID |
| 2 | 集中管理器 | `QrAddControllerDialogFrag` | 目前都是顶级设备，只需名称 + code |
| 3 | 单灯控制器 | `QrAddLightDialogFrag` | 字段最多的添加流程 |

> `DeviceTypeUtil.getQrDeviceType(type)` 用于二维码信息区的「类型名」显示：1→"采集控制器"、2→"集中管理器"、3→"单灯控制器"。

### 1.2 单灯小类 `mSingleType`（单灯控制器再细分）

来自 `DeviceTypeUtil.getSingleType()`：

| 值 | 名称 |
|----|------|
| 101 | 市电常规型单灯（默认值） |
| 102 | 市电太阳能混合单灯 |
| 103 | 市电带倾斜单灯 |
| 104 | 双色温单灯 |
| 105 | 485 单灯 |

### 1.3 其它常量

| 常量 | 值 | 含义 |
|------|----|------|
| `TYPE_CONCENTRATOR` | 2 | 集中器（获取站点集中器列表时用） |
| `TYPE_LIGHT` | 199 | 单灯（业务/地图标注类型，与本流程无直接关系） |
| `NODE_TYPE` | 2 | 站点节点类型 |
| `TYPE_BOX` | 0 | 配电箱地图类型 |
| `TYPE_POLE` | 14 | 灯杆地图类型 |
| `REQUEST_CODE_SCAN` | 129 | 扫码请求码 |
| `ALARM_UPLOAD` | `"alarm/upload"` | 灯杆二维码内容包含该字符串则判定为「灯杆二维码」 |
| `QR_LIGHT_JSON` | `"qr_light_json"` | 非顶级单灯复用缓存 key |
| `QR_LIGHT_TOP_JSON` | `"qr_top_light_json"` | 顶级单灯复用缓存 key |
| `POLE_BEAN` / `POLE_KEY` / `COORDINATE` | 事件 key | 跨页面/跨组件消息通道（见 §13） |

---


## 3. 功能入口与导航

两个页面（`SingleDetailActivity` 单灯详情页、`PowerBoxActivity` 配电箱详情页）都支持扫码添加和手动添加。


---

## 4. 总体流程时序

```
扫码入口 / 手动入口
        │
        ├─ 手动：HandleAddDialogFrag 输入 8 位设备ID → handleDeviceCode(code)
        │
        └─ 扫码：initCameraPermission() → ScanUtil.startScan()
                  → onActivityResult 拿到 originalValue
                      ├─ 包含 "alarm/upload" → 灯杆二维码
                      │     ├─ PowerBox(mPoleFlag=true) → getQrPoleInfo(poleKey) → QrAddPoleDialogFrag
                      │     └─ 否则/单灯页 → 存 mQrCode + 发 POLE_KEY 事件（给已打开的单灯弹窗用）
                      └─ 否则 → 设备二维码：清理换行/"ID" → mQrCode → getQrInfo(qrCode)

getQrInfo(qrCode)  →  POST /station/config/GetLightDeviceInfo {code}
        │
        └─ 返回 QrInfoBean.DataBean
              handleQrInfo(infoBean)
              ├─ isExist == true → 「设备已存在」提示 + 可选跳 FindDeviceIdActivity
              └─ isExist == false → 按 type 分发：
                    type 1/2 → QrAddControllerDialogFrag
                    type 3   → QrAddLightDialogFrag
```

---


iOS 对应：请求相机权限（`AVCaptureDevice.requestAccess`），允许后打开扫码界面（uniapp 的扫码组件）。

### 5.2 扫码结果解析（`onActivityResult`）

拿到二维码原始文本 `originalValue` 后：

1. **灯杆二维码判断**：`originalValue.contains("alarm/upload")`
   - 提取灯杆 key：`originalValue.substring(originalValue.lastIndexOf("=") + 1).replace("\n","").trim()`
   - `mQrCode = poleKey`
   - 分发：
     - `PowerBoxActivity` 且 `mPoleFlag == true` → `mDeviceViewModel.getQrPoleInfo(poleKey)`（校验灯杆 key 是否已在系统中）→ `QrAddPoleDialogFrag`
     - 其它（`mPoleFlag == false` 或单灯页）→ `sendMsg(POLE_KEY, poleKey)`，把 key 塞给已打开的单灯弹窗的「灯杆 ID」输入框
   2. **设备二维码**： `originalValue.！contains("alarm/upload")`
      - `qrCode = originalValue.replace("\n","").replace("ID","").trim()`
      - `mQrCode = qrCode`
      - `mDeviceViewModel.getQrInfo(qrCode)`（查询二维码对应设备信息）

> 注意：单灯添加弹窗内还有一个「灯杆扫码」按钮（`iv_qr_pole`），扫码后直接作为灯杆 key 填入，逻辑同「灯杆二维码」分支，但不请求接口。


---

## 6. 手动添加流程（输入 8 位设备 ID）

`HandleAddDialogFrag` 只有一个输入框，点击确定：

```kotlin
val deviceCode = editDeviceCode.text.trim()
if (deviceCode.isNotEmpty() && deviceCode.length == 8) {
    listener.handleDeviceCode(deviceCode)   // 校验：非空 且 长度 == 8
    dismiss()
} else {
    toast("输入8位设备ID")
}
```

`handleDeviceCode(code)`（两个页面一致）：

```kotlin
mQrCode = code
mDeviceViewModel.getQrInfo(code)   // 与扫码得到 code 后完全同一路径
```

> 结论：**手动添加 = 跳过扫码，直接用 8 位设备 ID 调 `getQrInfo`**，之后流程与扫码完全一致。

iOS 文案：提示「输入 8 位设备 ID」（英文 "Enter 8-digit Device ID"）。

---

## 7. 二维码信息查询接口

### 7.1 设备二维码信息 `getQrInfo`

- 路径：`POST /station/config/GetLightDeviceInfo`
- 请求体（业务 JSON）：

```json
{ "code": "<8位设备ID / 二维码code>" }
```

- 响应 → （GetLightDeviceInfo 响应）

```json
{
  "code": "",           // 设备 code
  "batch": 0,           // int 批次
  "deviceType": 0,      // int
  "isTop": false,       // boolean 是否顶级（关键字段）
  "name": "",           // 设备型号名（如 PLC 相关、控制器型号）
  "no": 0,              // int
  "protocol": "",       // 协议
  "type": 0,            // int 设备大类 1/2/3
  "year": 0,            // int
  "isExist": false,     // boolean 是否已存在（关键字段）
  "stationId": 0,       // int
  "deviceId": 0,        // int
  "stationName": ""     // 站点名（可能为 null）
}
```


### 8. 结果分发逻辑 handleQrInfo

两个页面逻辑几乎一致，差异见标注。

```kotlin
fun handleQrInfo(infoBean: QrInfoBean.DataBean) {


    if (infoBean.isExist) {
        // 设备已存在：弹「查找设备」确认框
        ConfirmedDialog("查找设备") { 跳转 FindDeviceIdActivity(带 ID = infoBean.code) }
        toast("设备已存在")
    } else {
        when (infoBean.type) {
            1, 2 -> 采集控制器 / 集中管理器
            3    -> 添加单灯
        }
    }
}
```

** 要点 **：
- `isExist` 为真 → 弹「设备已存在」+「查找设备」确认框，确认后进入「查找设备」页面（`FindDeviceIdActivity`，带参数 `id = code`）。
- `isExist` 为假 → 按 `type` 进入对应添加弹窗。

---

## 9. QrAddControllerDialogFrag（采集控制器 / 集中管理器）

### 9.1 构造参数

| 参数 | 说明 |
|------|------|
| `stationId` | 站点 ID |
| `stationName` | 站点名称（用于设备名默认值） |
| `qrCode` | 扫码/手输的设备 code（显示在「设备 ID」栏） |
| `bean` | `QrInfoBean.DataBean` |
| `listener` | `OnQrSingleListener`（`qrOk` / `qrOperation` 回调） |

### 9.2 显示逻辑（`initData`）

```
设备名输入框 editControllerName  ← 预填 mStationName
「保存/添加」按钮 btnSaveController ← 若 mBean.isExist 则隐藏，否则显示
「设备 ID」tvDeviceId ← mQrCode

if (mBean.type == 1) {          // 采集控制器
    显示 switchButtonQr（监测电压 / 总配电 开关，即 simple）
    if (mBean.isIsTop) {        // 顶级采集器
        隐藏「通信 ID」输入（tvController7 / llAddDevice7）
    } else {                    // 非顶级采集器
        显示「通信 ID」输入，提示输入 11 或 20 位通信 ID
    }
} else {                         // type == 2 集中管理器
    隐藏 switchButtonQr
    隐藏「通信 ID」输入
}
```

`qrInfoView.setData(mBean)` 显示二维码信息区（见 §16.2 及 `QrDeviceInfoView` 逻辑）：
- 类型名 = `getQrDeviceType(bean.type)`
- 二维码 ID = `bean.code`
- 设备型号 = `bean.name`
- 协议 = `bean.protocol`
- 描述 = `"${bean.year} - ${bean.batch} - ${bean.no}"`
- 顶部/非顶级标记 = `bean.isIsTop`（顶级用红色，非顶级用强调色；type==1 且非顶级额外显示提示「需要输入11位或者20位长度的通信ID」）

### 9.3 校验逻辑（`submit`）

```
deviceName = editControllerName
deviceId   = tvDeviceId (即 mQrCode)
connectId  = tvConnectId（通信ID输入框）

if (type == 1 && !isTop && (connectId.length != 11 && connectId.length != 20))
    → toast("请输入11位或20位长度的通信ID")
else if (deviceName.isEmpty())
    → toast("请输入设备名称")
else 提交
```

### 9.4 提交参数与接口

构造 `QrFastAddBean`（JSON 字段见 §16.1）：

```json
{
  "address": { "...": "..." },               // 来自 LocationUtil().getLocationBean()（当前定位逆地理）
  "stationId": 123,                 // mStationId.toInt()
  "name": "<deviceName>",
  "monitorId": "<connectId>",       // 通信ID（顶级/集中器时为空字符串）
  "code": "<deviceId>",
  "lat": 22.5, "lng": 113.9,        // MapApplication.mLocation 当前经纬度
  "simple": "true/false"              // switchButtonQr 开关值（监测电压/总配电）
}
```

调用：`mDeviceViewModel.addDeviceNew(json, 2)` → `POST /station/config/AddDevice`。

> 注意 `addDeviceNew(json, type)` 的 `type` 参数：`type==3` 走单灯结果回调，否则走通用结果回调。控制器走 `type=2`（非 3），所以走 `resultViewModel`。

### 9.5 结果回调

- 成功 → toast「添加成功」→ `mListener.qrOk(mBean.type)` → dismiss。
- 失败 → `showResult(errorBean)`。

---

## 10. QrAddLightDialogFrag（单灯控制器，重点）

### 10.1 构造参数

| 参数 | 说明 |
|------|--[build](build)----|
| `stationId` | 站点 ID |
| `qrCode` | 扫码/手输 code（`mQrCode`） |
| `bean` | `QrInfoBean.DataBean`（`mBean`） |
| `listener` | `OnQrSingleListener` |

### 10.2 初始化数据请求（`initData` 触发）

进入弹窗即并行请求：

| 请求 | 方法 | 用途 |
|------|------|------|
| `getPoleList(stationId)` | `POST /station/config/QueryLampPole {stationId}` | 站点灯杆列表 |
| `getLightGroup(stationId)` | `POST /station/config/QueryArea {stationId, groupId:0}` | 单灯分组列表 |
| `getStationDeviceNew(stationId, 2)` | `POST /station/config/GetStationDevice {stationId, type:2}` | 集中器列表（**仅非顶级单灯**） |

> 顶级单灯（`mBean.isIsTop == true`）不请求集中器列表，且隐藏「上级设备」和「集中器」标签。

### 10.3 显示逻辑

#### 10.3.1 通道默认值

```kotlin
// PLC 单灯默认通道 6，其它默认通道 2
val ch = if (mBean.name.contains("PLC")) "6" else "2"
editPoleChannel.setText(ch)
```

#### 10.3.2 复用缓存读取（重点，见 §12）

```kotlin
val qrLightJson = if (mBean.isIsTop) sp.get(QR_LIGHT_TOP_JSON) else sp.get(QR_LIGHT_JSON)
```

- **有缓存**：解析为 `QrLightJsonBean`，回填：
  - `mTimeId = lightBean.timeId`、`tvAssociatedTime = lightBean.timetableName`
  - `editControllerName = lightBean.lightName`
  - `editPower1..4 = lightBean.pr1..4`
  - `cb1..4 = lightBean.isEn1..4`
  - 若含 `"position"` 且站点相同 → `mPolePosition = lightBean.position`（列表滚动定位用）
  - 非顶级：若含 `"stationId"` 且站点相同 → 回填集中器 `tvParentDevice = lightBean.parentName`，`mDeviceId = lightBean.parentId`
  - `tvSingleType = lightBean.typeName`，`mSingleType = lightBean.type`
- **无缓存（初次添加）**：`cb1 = true`，4 路功率预填 `100`（`default_power`）。

#### 10.3.3 灯杆复用（`initPole`）

- 灯杆列表非空 → 显示灯杆选择箭头 `ivChoosePole`。
- 若缓存含 `poleId` 且站点相同 → 复用灯杆：`mPoleId = lightBean.poleId`、`editPoleKey = lightBean.poleName`。
- 灯杆列表为空 → 隐藏选择箭头（`ivChoosePole`）。

#### 10.3.4 分组复用（`initGroup`）

- 分组列表非空：
  - 若缓存含 `groupId` 且站点相同 → 复用：`mGroupId = lightBean.groupId`、`tvSingleGroup = lightBean.groupName`
  - 否则（无缓存 / 站点不同）→ 选「默认分组」，没有则选第一个：`tvSingleGroup = groupBean.name`、`mGroupId = groupBean.id`
  - 同时填充 `mGroupList`（名称列表）和 `mGroupIdMap`（name→id）。

#### 10.3.5 集中器选择（`deviceViewModel` 回调）

- 集中器列表返回后：`mDeviceNameMap[code]=name`、`mDeviceIdMap[name]=code`、`mDeviceList += name`。
- 判断复用的 `parentId` 是否在列表中（`mDeviceNameMap[lightBean.parentId]`）：
  - 不在 → `showDevice()` 弹出集中器选择列表，选中后 `mDeviceId = mDeviceIdMap[name]`、`tvParentDevice = name`
  - 在 → 直接使用上次保存的集中器（不弹窗）

#### 10.3.6 时间表选择（`newTimeViewModel` 回调）

- 点击「时间表」→ `timetableViewModel.getLightTimetable()` → `POST /station/plan/QueryLightPlan`（无 body）。
- 返回 `NewTimetableBean.DataBean[]`，显示列表项名称为 `"${bean.type} ${bean.name}"`，选中后 `mTimeId = bean.id`、`tvAssociatedTime = content`。

#### 10.3.7 控制通道选择（`outputViewModel` 回调）

- 点击「控制输出」→ `getOutputList(stationId)` → `POST /station/config/QueryOutput/ {groupId:0, stationId}`。
- 列表首项固定加「无」（default_null），选中「无」→ `mOutputId = 0`，否则 `mOutputId = outputList[position].id`。

#### 10.3.8 单灯类型选择

- 点击「单灯类型」→ `DeviceTypeUtil.getSingleType()` 提供 5 个选项（§1.2）。
- 选中后 `mSingleType = idMap[name]`、`tvSingleType = name`。

### 10.4 错误状态判断（`errorLiveData`）

```kotlin
if (errorUrl 包含 "/station/config/GetStationDevice") {
    // 站点没有集中器
    tvParentDevice = ""
    toast("当前站点没有集中器,请先添加。")
    延迟1.5s → listener.qrOk(6) → dismiss()     // 6 = 提示去添加集中器
}
else if (errorUrl 包含 "/station/config/QueryLampPole") {
    toast("当前站点没有灯杆")
}
else showResult(errorBean)
```

### 10.5 灯杆选择 / 定位 / 扫码（交互）

| 交互 | 行为 |
|------|------|
| 点击「灯杆」行 `ll_pole` | 若灯杆列表空 → toast「没有发现灯杆」；否则打开 `SearchPoleDialog`（支持搜索，传入 `mPolePosition` 定位滚动） |
| 点击地图图标 `iv_choose_pole_map` | 打开 `ChoosePointMapActivity`（`type=4` 选择灯杆定位），选定后通过 `COORDINATE` 事件回传经纬度 |
| 点击灯杆扫码 `iv_qr_pole` | `ScanUtil.startScan` 扫灯杆二维码，key 填入 `editPoleKey` |
| 点击图片背景 `view_iv_bg` | 申请相册权限 → 选图 → 展示缩略图列表（可删除） |

灯杆选择结果通过三个事件回传（见 §13）：

```kotlin
COORDINATE → mLat = startLatitude, mLng = startLongitude       // 地图选点
POLE_BEAN  → mPoleId = id, mLat = lat, mLng = lng,
             mPolePosition = position, editPoleKey = name       // SearchPoleDialog 选中灯杆
POLE_KEY   → editPoleKey = 灯杆key                              // 扫码灯杆
```

### 10.6 提交校验（`submit`，按顺序）

```kotlin
val deviceName = editControllerName.text
val parentName = tvParentDevice.text
val timeName   = tvAssociatedTime.text
val power1..4  = editPower1..4
val singleType = tvSingleType.text
val poleKey    = editPoleKey.text
val groupName  = tvSingleGroup.text
val channel    = editPoleChannel.text

when {
    parentName.isEmpty() && !isTop            -> "请选择集中器备"      // （原文如此）
    // timeName.isEmpty() 已注释，时间表非必填
    singleType.isEmpty()                      -> "请选择单灯类型"
    // oid.isEmpty() 已注释，控制通道非必填
    channel.isEmpty()                         -> "请输入通道参数"
    poleKey.isEmpty()                         -> "添加灯杆ID或者输入灯杆名称"
    power1.isEmpty()                          -> "请选输入功率"
    cb1..cb4 全未勾选                         -> "至少启用一路控制"
    deviceName.isEmpty()                      -> "请输入单灯名称"
    mGroupList.isNotEmpty() && groupName.isEmpty() -> "请选择单灯分组"
    else -> 提交
}
```

### 10.7 提交参数（`QrFastAddBean`，`addDeviceNew(json, 3)`）

```json
{
  "address": { "...": "..." },              // LocationUtil().getLocationBean()
  "stationId": 123,
  "name": "<poleKey>-<deviceName>",   // 注意：设备名 = 灯杆key + "-" + 输入名
  "code": "<mQrCode>",               // 扫码/手输的设备 code
  "monitorId": "<mDeviceId>",        // 集中器 code（顶级单灯为 "0"）
  "areaId": 456,                     // 分组 id（mGroupId）
  "timeId": 789,                     // 时间表 id（mTimeId，可为 0）
  "type": 101,                       // 单灯小类 mSingleType
  "channel": 6,                      // 通道（PLC=6，其它=2）
  "oid": 0,                          // 控制输出 id（mOutputId，0=无）
  "pr1": 100.0, "en1": true,
  "pr2": 100.0, "en2": false,
  "pr3": 100.0, "en3": false,
  "pr4": 100.0, "en4": false,
  "lat": 22.5, "lng": 113.9,         // 灯杆位置（见 §13）
  "pole": "<poleKey>",               // 灯杆 key / 名称
  "poleId": 111                      // 灯杆 id（mPoleId，未选灯杆为 0）
}
```

调用：`mDeviceViewModel.addDeviceNew(json, 3)` → `POST /station/config/AddDevice`。

### 10.8 提交后回调

```kotlin
// 单灯添加成功回调 lightResultViewModel (QrLightResultBean)
if (mPicUriList.isNotEmpty()) {
    picViewModel.postStationPic(bean.poleId.toString(), POLE_PIC, mPicUriList)  // 先传图
} else {
    toast("添加成功")
    listener.qrOk(mBean.type)
    dismiss()
}

// 图片上传成功回调 picResultViewModel
toast("添加成功")
listener.qrOk(mBean.type)
dismiss()
```

> `QrLightResultBean` 字段：`lightId`、`poleId`、`isNewPole`（`isNewPole` 表示是否新建了灯杆，`poleId` 用于后续上传灯杆图片）。

### 10.9 清除复用缓存（长按二维码信息区）

长按 `qrInfoView` → 清空对应缓存 key（顶级清 `QR_LIGHT_TOP_JSON`，非顶级清 `QR_LIGHT_JSON`）→ toast「配置已清除」。

---

## 11. QrAddPoleDialogFrag（灯杆）

仅 `PowerBoxActivity` 在扫到灯杆二维码且 `getQrPoleInfo` 成功时弹出。

### 11.1 显示逻辑

```
tvPoleName   ← mBean.name
tvPoleType   ← mBean.asset.poleType
tvPoleHeight ← mBean.asset.height + " 米"
```

### 11.2 提交参数（`QrAddPoleBean`，`qrAddPole(json)`）

```json
{
  "address": { "...": "..." },
  "stationId": 123,
  "code": "<mQrCode>",      // 灯杆 key
  "lat": 22.5, "lng": 113.9
}
```

调用：`mDeviceViewModel.qrAddPole(json)` → `POST /station/config/LampPoleBuild`。

> 注意：`QrAddPoleBean` 只有 `lat/lng/stationId/code/address` 五个字段，**不提交名称/类型/高度**，名称类型高度仅作展示。

---

## 12. 参数复用（重复添加记忆）机制

核心：**把上一次成功添加的配置写入本地缓存（SpUtil / NSUserDefaults 等价物），下次打开弹窗时回填**。

### 12.1 缓存 Key

| Key | 对应场景 |
|-----|---------|
| `qr_light_json` | 非顶级单灯（需集中器） |
| `qr_top_light_json` | 顶级单灯 |

### 12.2 缓存内容 `QrLightJsonBean`

```json
{
  "stationId": "123",          // 站点 id（字符串）
  "parentId": "c229",          // 集中器 code（非顶级才有）
  "parentName": "集中器名称",
  "timetableName": "时间表名",
  "timeId": 789,
  "type": 101,                 // 单灯小类
  "channel": 6,
  "oc": 0,                     // 控制输出 id
  "poleId": 111,
  "poleName": "灯杆key",
  "typeName": "市电常规型单灯",
  "pr1": 100.0, "pr2": 100.0, "pr3": 100.0, "pr4": 100.0,
  "en1": true, "en2": false, "en3": false, "en4": false,
  "lightName": "输入名",
  "groupId": 456,
  "groupName": "默认分组",
  "position": 3                // 上次所选灯杆在列表中的位置（滚动定位）
}
```

### 12.3 复用规则（关键：跨站点判断）

回填前一律先判断 `stationId` 是否等于当前站点：

| 字段 | 复用条件 |
|------|---------|
| 灯杆位置 `position` | `qrLightJson.contains("position")` 且 `stationId == 当前站点` |
| 集中器 `parentId/parentName` | `qrLightJson.contains("stationId")` 且 `stationId == 当前站点` |
| 灯杆 `poleId/poleName` | `qrLightJson.contains("poleId")` 且 `stationId == 当前站点` |
| 分组 `groupId/groupName` | `qrLightJson.contains("groupId")` 且 `stationId == 当前站点` |

- 站点相同 → 完全复用上次选择。
- 站点不同 → 位置归 0、集中器/灯杆不预填、分组回退到「默认分组或第一个」。

### 12.4 保存时机

提交成功前（点击「添加」时，在调 `addDeviceNew` 之前）就把当前配置写入缓存（顶级写 `qr_top_light_json`，非顶级写 `qr_light_json`，非顶级额外写 `parentId/parentName`）。

> 语义上这是「记住本次填写的表单」，供下一次「重复添加」快速复用，而非等待接口成功。

---

## 13. 灯杆位置提交说明

**结论：单灯添加流程中，灯杆位置（经纬度）是随 `AddDevice` 接口一起提交的（JSON 里的 `lat` / `lng`），没有单独的位置提交接口。**

灯杆位置有三个来源，优先级由交互决定：

| 来源 | 事件 | 赋值 |
|------|------|------|
| 当前定位（默认） | 初始化 `initLocation()` | `mLat/mLng = MapApplication.mLocation`（定位失败为 0.0） |
| 地图选点 | `COORDINATE` 事件（`ChoosePointMapActivity` type=4） | `mLat = MapPointBean.startLatitude`、`mLng = startLongitude` |
| 灯杆列表选中 | `POLE_BEAN` 事件（`SearchPoleDialog`） | `mLat = pole.lat`、`mLng = pole.lng`、`mPoleId = pole.id` |

三者最终都写进 `QrFastAddBean.lat/lng`，随 `POST /station/config/AddDevice` 提交。

另外：
- `address` 字段来自 `LocationUtil().getLocationBean()`（当前定位的逆地理信息），也是随 `AddDevice` 提交，**不随地图选点更新**。
- 已有灯杆/配电箱的「修改经纬度」走的是 `POST /station/gis/SetPos`（`ChoosePointMapActivity` 的 `TYPE_BOX`/`TYPE_POLE` 分支），这属于灯杆管理/配电箱定位功能，**不在添加设备流程内**。

---

## 14. 灯杆图片上传

单灯弹窗支持选图（相册多选），添加成功后若选了图：

```kotlin
picViewModel.postStationPic(bean.poleId.toString(), Constant.POLE_PIC, mPicUriList)
```

- 触发时机：`AddDevice` 成功（拿到 `poleId`）后。
- 图片类型：`POLE_PIC`（灯杆图片）。
- 接口：图片上传走 `multipart/form-data`，绑定灯杆图片相关接口为 `/station/config/BindPoleImages`（`BIND_POLE_PIC`，供参考）。

---

## 15. 接口清单与请求体

| # | 路径 | 方法 | 用途 | 请求体（业务 JSON） |
|---|------|------|------|---------------------|
| 1 | `/station/config/GetLightDeviceInfo` | POST | 二维码对应设备信息 | `{"code": "<code>"}` |
| 2 | `/station/alarm/QueryUploadAlarm` | POST | 灯杆二维码校验 | `{"type":"pole","code":"<poleKey>"}` |
| 3 | `/station/config/AddDevice` | POST | 扫码/手动添加设备 | `QrFastAddBean`（§16.1） |
| 4 | `/station/config/LampPoleBuild` | POST | 扫码添加灯杆 | `QrAddPoleBean`（§16.4） |
| 5 | `/station/config/QueryLampPole` | POST | 站点灯杆列表 | `{"stationId":"<id>"}` |
| 6 | `/station/config/QueryArea` | POST | 单灯分组列表 | `{"stationId":"<id>","groupId":0}` |
| 7 | `/station/config/GetStationDevice` | POST | 站点设备列表（集中器） | `{"stationId":"<id>","type":2}` |
| 8 | `/station/config/QueryOutput/` | POST | 站点通道列表 | `{"groupId":0,"stationId":"<id>"}` |
| 9 | `/station/plan/QueryLightPlan` | POST | 单灯时间表列表 | `{}`（无 body） |
| 10 | `/station/config/FastSaveLight` | POST | 快速添加单灯（备用，当前两页未使用） | `QrFastAddBean` |
| 11 | `/station/config/BindPoleImages` | POST | 绑定灯杆图片 | multipart |

> 请求体/响应统一走 §2 的 Base64 包装与解密。

---

## 16. 数据结构 JSON 明细

> 以下字段名即 Gson 序列化后的 JSON key（iOS 端务必按此命名）。

### 16.1 `QrFastAddBean`（AddDevice 请求体）

```json
{
  "areaId": 0,        // int，单灯分组 id
  "code": "",         // String，设备 code
  "monitorId": "",    // String，上级设备（集中器）code
  "lat": 0.0,         // double
  "lng": 0.0,         // double
  "name": "",         // String，设备名（单灯=灯杆key-输入名）
  "pr1": 0.0, "en1": false,
  "pr2": 0.0, "en2": false,
  "pr3": 0.0, "en3": false,
  "pr4": 0.0, "en4": false,
  "simple": false,    // boolean，采集器是否「监测电压/总配电」
  "stationId": 0,     // int
  "timeId": 0,        // int
  "type": 0,          // int，设备大类/单灯小类
  "channel": 0,       // int
  "oid": 0,           // int，控制输出 id
  "pole": "",         // String，灯杆 key/名称
  "poleId": 0,        // int
  "address": {        // AddressBean
    "adcode":"", "address":"", "city":"", "cityCode":"",
    "country":"", "countryCode":"", "district":"",
    "province":"", "street":"", "streetNumber":"", "town":""
  }
}
```

```

### 16.3 `QrLightResultBean`（AddDevice 单灯响应）

```json
{ "lightId": 0, "poleId": 0, "isNewPole": false }
```

### 16.4 `QrAddPoleBean`（LampPoleBuild 请求体）

```json
{
  "lat": 0.0, "lng": 0.0,
  "stationId": 0,
  "code": "",
  "address": { "...": "..." }
}
```

### 16.5 `QrPoleInfoBean`（QueryUploadAlarm 响应）

```json
{
  "id": 0,
  "name": "",
  "asset": { "poleType": "", "armType": "", "height": 0 },
  "address": "",
  "lat": 0.0, "lng": 0.0,
  "buildDate": ""
}
```

### 16.6 列表类结构

- **灯杆列表** `PoleListBean`（QueryLampPole 响应）：`id, name, stationId, stationName, lat, lng, position`
- **分组列表** `LightGroupBean`（QueryArea 响应）：`id, name, code`
- **集中器/设备列表** `DeviceDetailBean`（GetStationDevice 响应）：关键字段 `id, name, code`（另有 type/typeName/channel 等）
- **时间表** `NewTimetableBean.DataBean`（QueryLightPlan 响应）：`id, name, type(字符串), mode`
- **通道列表** `NewGroupControlBean.DataData`（QueryOutput 响应）：关键字段 `id, name`

---

## 17. iOS 实现对照清单

1. **网络层**：实现 Base64 包装（`{"content": Base64(json)}`）+ 4 个请求头 + 响应解密。
2. **扫码**：相机权限 → 扫码 → 拿原始文本 → 按 §5.2 解析（`alarm/upload` 灯杆分支 / 设备分支）。
3. **手动添加**：输入 8 位设备 ID → 复用 `getQrInfo` 路径。
4. **信息查询**：`GetLightDeviceInfo` 返回 `isExist/type/isTop` → 分发（§8）。
5. **控制器弹窗**（type 1/2）：实现 §9 的显示/校验/提交（字段少）。
6. **单灯弹窗**（type 3）：实现 §10 全流程：
   - 初始化 3 个列表请求（灯杆/分组/集中器）
   - 复用缓存回填（§12）
   - 通道默认值（PLC=6，其它=2）
   - 灯杆三来源（地图/列表/扫码）
   - 分组/时间表/通道/类型选择
   - 校验 + 提交 `AddDevice` + 图片上传
7. **灯杆弹窗**（配电箱页灯杆二维码）：§11。
8. **本地缓存**：两个复用 key，跨站点判断，长按清除。
9. **回调**：`qrOk(type)` → 单灯页刷新；配电箱页弹「添加后操作」弹窗（type=3 查看单灯 / type=6 添加集中器 / 其它查看设备，可继续添加）。

### iOS 文案对照（关键）

| 场景 | 文案 |
|------|------|
| 手动添加 | 输入 8 位设备 ID |
| 设备已存在 | 设备已存在 / 查找设备 |
| 无集中器 | 当前站点没有集中器,请先添加。 |
| 无灯杆 | 当前站点没有灯杆 |
| 未选集中器 | 请选择集中器备 |
| 未选类型 | 请选择单灯类型 |
| 通道为空 | 请输入通道参数 |
| 灯杆为空 | 添加灯杆ID或者输入灯杆名称 |
| 功率为空 | 请选输入功率 |
| 未启用任一路 | 至少启用一路控制 |
| 名称为空 | 请输入单灯名称 |
| 未选分组 | 请选择单灯分组 |
| 通信 ID 长度错误 | 请输入11位或20位长度的通信 ID |
| 清除缓存 | 配置已清除 |
| 添加成功 | 添加成功 |

---

## 18. 注意事项与易踩坑点

1. **`name` 的拼接**：单灯提交的 `name = "<poleKey>-<deviceName>"`，不是纯输入名。
2. **`monitorId` 语义**：单灯弹窗里 `monitorId` 存的是集中器的 `code`（通信 ID），顶级单灯为 `"0"`（字符串）。
3. **通道默认值**：仅看 `mBean.name` 是否含 `"PLC"`（大小写敏感）决定 6 还是 2。
4. **时间表/控制通道非必填**：校验里 `timeName`、`oid` 相关判断都被注释掉了，`timeId` 可为 0，`oid` 可为 0。
5. **复用缓存跨站点**：所有复用回填前都要判断 `stationId`，否则会把 A 站点的集中器/灯杆/分组错填到 B 站点。
6. **`stationId` 类型**：请求接口时 `stationId` 要转 int（`toInt()`），但列表请求用字符串（`stationId` 直接 put 字符串）。
7. **灯杆位置**：没有单独提交接口，随 `AddDevice` 的 `lat/lng` 一起提交；`address` 只取当前定位，不随地图选点变化。
8. **`isExist` 双保险**：控制器弹窗里 `isExist` 时隐藏「添加」按钮；分发时 `isExist` 已弹「设备已存在」，所以正常不会走到这里。
9. **添加成功回调分流**：`addDeviceNew(json, type)` 的 `type==3` 才回 `QrLightResultBean`（含 `poleId`，用于传图）；控制器/灯杆走通用 `resultViewModel`。
10. **灯杆二维码 vs 设备二维码**：判定条件是原始文本是否包含 `alarm/upload`；灯杆 key 取 `lastIndexOf("=")` 之后，设备 code 要去掉换行和 `"ID"` 前缀。
11. **请求体字段顺序无关**，但 key 名必须与 §16 完全一致（后端按字段名反序列化）。

---

